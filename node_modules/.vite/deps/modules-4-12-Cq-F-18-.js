import { n as __exportAll } from "./rolldown-runtime-DC62tzP2.js";
import { Bt as setMeta, Ft as Meta, It as UNDEFINED, Kt as getOwner, Lt as counters, Rt as meta, Wt as setOwner, _ as environment_exports, zt as peekMeta } from "./set_properties-BghuuGJx-CRFenNvF.js";
import { i as owner_exports } from "./router-nu9yFjqm.js";
import { a as observable_exports, r as object_exports, s as core_exports } from "./object-DGleR2Z8.js";
import { a as debug_exports } from "./debug-BQ1Lq6xe.js";
import { d as schedule, u as runloop_exports } from "./runloop-Ds2WWrox.js";
import { a as isIndexable, c as getFirst, i as isDict, l as getLast, n as dict, t as StackImpl, u as isPresentArray } from "./collections-DPkjqeA3-BTZFUfs_.js";
import { R as createCache, S as valueForRef, W as getValue, ct as registerDestructor, d as createComputeRef, ht as toBool, nt as associateDestroyableChild, ot as isDestroyed, rt as destroy, st as isDestroying } from "./args-proxy-BNLuRFlO-EccNNvt4.js";
import { n as version_exports } from "./version-IM4DJ0Kp.js";
import { $ as async, F as controller_exports, H as mutable_exports$1, J as RSVP, K as EventTarget, Q as asap, R as MutableArray, X as all, Z as allSettled, at as hash, ct as off, dt as reject, et as cast, ft as resolve, it as filter, lt as on, nt as defer, ot as hashSettled, pt as rethrow, q as Promise$1, rt as denodeify, st as map, tt as configure, ut as race, z as array_exports } from "./route-6MoB3WQU.js";
import { A as ConcreteBounds, D as reifyNamed, E as reifyArgs, M as clear, O as reifyPositional, S as curry, d as CurriedValue, h as EMPTY_POSITIONAL, i as on$1, j as CursorImpl, m as EMPTY_NAMED, p as EMPTY_ARGS, s as instrumentation_exports, x as createCapturedArgs } from "./internal-CAL-jHZt-CnT936Oy.js";
import { h as normalizeProperty } from "./textarea-lD4apgrP-C3dwQyUU.js";
import { n as evented_exports } from "./evented-2vnwjt81.js";
import { a as hasDestroyable, l as hasValue, r as getInternalHelperManager } from "./api-BawZUDYD-Cxxta-tL.js";
import { $ as DynamicAttribute, B as LowLevelVM, C as array, E as internalHelper, G as renderComponent, H as UpdatingVM, I as DOMChanges, J as resetDebuggerCallback, K as renderMain, L as DOMChangesImpl, Q as DOMTreeConstruction, R as DynamicScopeImpl, S as get, T as hash$1, U as inTransaction, V as ScopeImpl, W as isWhitespace, X as setDebuggerCallback, Y as runtimeOptions, Z as reference_exports, at as dynamicAttribute, c as RehydrateTree, d as rehydrationBuilder, et as NewTreeBuilder, f as namespace_exports, it as clientBuilder, l as SERIALIZATION_FIRST_NODE_STRING$1, n as application_exports, nt as ResettableBlockImpl, p as utils_exports, q as renderSync, rt as SimpleDynamicAttribute, tt as RemoteBlock, u as isSerializationFirstNode$1, w as fn, x as concat, z as EnvironmentImpl } from "./application-CJjdcrNQ.js";
import { r as service_exports } from "./service-x9qJ7oMe.js";
import { C as entries, S as assign, T as values, _ as enumerate, b as zipArrays, g as emptyArray, h as EMPTY_STRING_ARRAY, m as EMPTY_NUMBER_ARRAY, p as EMPTY_ARRAY, v as isEmptyArray, w as keys, x as zipTuples, y as reverse } from "./index-CyZiGiEI-CTG3K8jV.js";
import { r as componentCapabilities } from "./api-BAmlf_CN-DtAd_Q7k.js";
import { i as templateOnlyComponent, n as TemplateOnlyComponentDefinition, r as TemplateOnlyComponentManager, t as TEMPLATE_ONLY_COMPONENT_MANAGER } from "./template-only-BqOt8bDl-Dt4RHISl.js";
import { n as component_exports, r as setComponentManager } from "./component-DexvXWbn.js";
import { n as views_exports, r as runtime_exports$1, t as manager_exports } from "./manager-B_XH8om1.js";
import { t as destroyable_exports } from "./destroyable-Be65LxUB.js";
import { n as metal_exports, t as validator_exports } from "./validator-D5rFj32N.js";
import { i as proxy_exports$1, n as proxy_exports, o as promise_proxy_mixin_exports } from "./proxy-B3zfIjMt.js";
import { t as internals_exports } from "./internals-DePGURBK.js";
//#region node_modules/ember-source/dist/dev/packages/@ember/array/mutable.js
var mutable_exports = /* @__PURE__ */ __exportAll({ default: () => MutableArray });
//#endregion
//#region node_modules/ember-source/dist/dev/packages/@ember/-internals/meta/index.js
var meta_exports = /* @__PURE__ */ __exportAll({
	Meta: () => Meta,
	UNDEFINED: () => UNDEFINED,
	counters: () => counters,
	meta: () => meta,
	peekMeta: () => peekMeta,
	setMeta: () => setMeta
});
//#endregion
//#region node_modules/ember-source/dist/dev/packages/rsvp/index.js
var rsvp_exports = /* @__PURE__ */ __exportAll({
	EventTarget: () => EventTarget,
	Promise: () => Promise$1,
	all: () => all,
	allSettled: () => allSettled,
	asap: () => asap,
	async: () => async,
	cast: () => cast,
	configure: () => configure,
	default: () => RSVP,
	defer: () => defer,
	denodeify: () => denodeify,
	filter: () => filter,
	hash: () => hash,
	hashSettled: () => hashSettled,
	map: () => map,
	off: () => off,
	on: () => on,
	race: () => race,
	reject: () => reject,
	resolve: () => resolve,
	rethrow: () => rethrow
});
//#endregion
//#region node_modules/@glimmer/component/dist/index.js
var dist_exports = /* @__PURE__ */ __exportAll({ default: () => Component });
var DESTROYING = /* @__PURE__ */ new WeakMap();
var DESTROYED = /* @__PURE__ */ new WeakMap();
function setDestroying(component) {
	DESTROYING.set(component, true);
}
function setDestroyed(component) {
	DESTROYED.set(component, true);
}
var ARGS_SET = /* @__PURE__ */ new WeakMap();
/**
* This provides us a way to have a "fallback" which represents an empty object,
* without the downsides of how TS treats `{}`. Specifically: this will
* correctly leverage "excess property checking" so that, given a component
* which has no named args, if someone invokes it with any named args, they will
* get a type error.
*
* @internal This is exported so declaration emit works (if it were not emitted,
*   declarations which fall back to it would not work). It is *not* intended for
*   public usage, and the specific mechanics it uses may change at any time.
*   The location of this export *is* part of the public API, because moving it
*   will break existing declarations, but is not legal for end users to import
*   themselves, so ***DO NOT RELY ON IT***.
*/
/** Given a signature `S`, get back the `Args` type. */
/**
* Given any allowed shorthand form of a signature, desugars it to its full
* expanded type.
*
* @internal This is only exported so we can avoid duplicating it in
*   [Glint](https://github.com/typed-ember/glint) or other such tooling. It is
*   *not* intended for public usage, and the specific mechanics it uses may
*   change at any time. Although the signature produced by is part of Glimmer's
*   public API the existence and mechanics of this specific symbol are *not*,
*   so ***DO NOT RELY ON IT***.
*/
/**
* @internal we use this type for convenience internally; inference means users
*   should not normally need to name it
*/
/**
* The `Component` class defines an encapsulated UI element that is rendered to
* the DOM. A component is made up of a template and, optionally, this component
* object.
*
* ## Defining a Component
*
* To define a component, subclass `Component` and add your own properties,
* methods and lifecycle hooks:
*
* ```ts
* import Component from '@glimmer/component';
*
* export default class extends Component {
* }
* ```
*
* ## Lifecycle Hooks
*
* Lifecycle hooks allow you to respond to changes to a component, such as when
* it gets created, rendered, updated or destroyed. To add a lifecycle hook to a
* component, implement the hook as a method on your component subclass.
*
* For example, to be notified when Glimmer has rendered your component so you
* can attach a legacy jQuery plugin, implement the `didInsertElement()` method:
*
* ```ts
* import Component from '@glimmer/component';
*
* export default class extends Component {
*   didInsertElement() {
*     $(this.element).pickadate();
*   }
* }
* ```
*
* ## Data for Templates
*
* `Component`s have two different kinds of data, or state, that can be
* displayed in templates:
*
* 1. Arguments
* 2. Properties
*
* Arguments are data that is passed in to a component from its parent
* component. For example, if I have a `UserGreeting` component, I can pass it
* a name and greeting to use:
*
* ```hbs
* <UserGreeting @name="Ricardo" @greeting="Olá" />
* ```
*
* Inside my `UserGreeting` template, I can access the `@name` and `@greeting`
* arguments that I've been given:
*
* ```hbs
* {{@greeting}}, {{@name}}!
* ```
*
* Arguments are also available inside my component:
*
* ```ts
* console.log(this.args.greeting); // prints "Olá"
* ```
*
* Properties, on the other hand, are internal to the component and declared in
* the class. You can use properties to store data that you want to show in the
* template, or pass to another component as an argument.
*
* ```ts
* import Component from '@glimmer/component';
*
* export default class extends Component {
*   user = {
*     name: 'Robbie'
*   }
* }
* ```
*
* In the above example, we've defined a component with a `user` property that
* contains an object with its own `name` property.
*
* We can render that property in our template:
*
* ```hbs
* Hello, {{user.name}}!
* ```
*
* We can also take that property and pass it as an argument to the
* `UserGreeting` component we defined above:
*
* ```hbs
* <UserGreeting @greeting="Hello" @name={{user.name}} />
* ```
*
* ## Arguments vs. Properties
*
* Remember, arguments are data that was given to your component by its parent
* component, and properties are data your component has defined for itself.
*
* You can tell the difference between arguments and properties in templates
* because arguments always start with an `@` sign (think "A is for arguments"):
*
* ```hbs
* {{@firstName}}
* ```
*
* We know that `@firstName` came from the parent component, not the current
* component, because it starts with `@` and is therefore an argument.
*
* On the other hand, if we see:
*
* ```hbs
* {{name}}
* ```
*
* We know that `name` is a property on the component. If we want to know where
* the data is coming from, we can go look at our component class to find out.
*
* Inside the component itself, arguments always show up inside the component's
* `args` property. For example, if `{{@firstName}}` is `Tom` in the template,
* inside the component `this.args.firstName` would also be `Tom`.
*/
var GlimmerComponent = class {
	/**
	* Constructs a new component and assigns itself the passed properties. You
	* should not construct new components yourself. Instead, Glimmer will
	* instantiate new components automatically as it renders.
	*
	* @param owner
	* @param args
	*/
	constructor(owner, args) {
		if (!(owner !== null && typeof owner === "object" && ARGS_SET.has(args))) throw new Error(`You must pass both the owner and args to super() in your component: ${this.constructor.name}. You can pass them directly, or use ...arguments to pass all arguments through.`);
		this.args = args;
		DESTROYING.set(this, false);
		DESTROYED.set(this, false);
	}
	/**
	* Named arguments passed to the component from its parent component.
	* They can be accessed in JavaScript via `this.args.argumentName` and in the template via `@argumentName`.
	*
	* Say you have the following component, which will have two `args`, `firstName` and `lastName`:
	*
	* ```hbs
	* <my-component @firstName="Arthur" @lastName="Dent" />
	* ```
	*
	* If you needed to calculate `fullName` by combining both of them, you would do:
	*
	* ```ts
	* didInsertElement() {
	*   console.log(`Hi, my full name is ${this.args.firstName} ${this.args.lastName}`);
	* }
	* ```
	*
	* While in the template you could do:
	*
	* ```hbs
	* <p>Welcome, {{@firstName}} {{@lastName}}!</p>
	* ```
	*/
	args;
	get isDestroying() {
		return DESTROYING.get(this) || false;
	}
	get isDestroyed() {
		return DESTROYED.get(this) || false;
	}
	/**
	* Called before the component has been removed from the DOM.
	*/
	willDestroy() {}
};
var BaseComponentManager = class {
	owner;
	constructor(owner) {
		this.owner = owner;
	}
	createComponent(ComponentClass, args) {
		ARGS_SET.set(args.named, true);
		return new ComponentClass(this.owner, args.named);
	}
	getContext(component) {
		return component;
	}
};
var CAPABILITIES = componentCapabilities("3.13", {
	destructor: true,
	asyncLifecycleCallbacks: false,
	updateHook: false
});
function scheduledDestroyComponent(component) {
	if (component.isDestroyed) return;
	destroy(component);
	setDestroyed(component);
}
/**
* This component manager runs in Ember.js environments and extends the base component manager to:
*
* 1. Properly destroy the component's associated `meta` data structure
* 2. Schedule destruction using Ember's runloop
*/
var EmberGlimmerComponentManager = class extends BaseComponentManager {
	capabilities = CAPABILITIES;
	destroyComponent(component) {
		if (component.isDestroying) return;
		setDestroying(component);
		schedule("actions", component, component.willDestroy);
		schedule("destroy", this, scheduledDestroyComponent, component);
	}
};
/**
A component is a reusable UI element that consists of a `.hbs` template and an
optional JavaScript class that defines its behavior. For example, someone
might make a `button` in the template and handle the click behavior in the
JavaScript file that shares the same name as the template.

Components are broken down into two categories:

- Components _without_ JavaScript, that are based only on a template. These
are called Template-only or TO components.
- Components _with_ JavaScript, which consist of a template and a backing
class.

Ember ships with two types of JavaScript classes for components:

1. Glimmer components, imported from `@glimmer/component`, which are the
default components for Ember Octane (3.15) and more recent editions.
2. Classic components, imported from `@ember/component`, which were the
default for older editions of Ember (pre 3.15).

Below is the documentation for Template-only and Glimmer components. If you
are looking for the API documentation for Classic components, it is
[available here](/ember/release/classes/Component). The source code for
Glimmer components can be found in [`@glimmer/component`](https://github.com/glimmerjs/glimmer.js/tree/master/packages/%40glimmer/component).

## Defining a Template-only Component

The simplest way to create a component is to create a template file in
`app/templates/components`. For example, if you name a template
`app/templates/components/person-profile.hbs`:

```app/templates/components/person-profile.hbs
<h1>{{@person.name}}</h1>
<img src={{@person.avatar}}>
<p class='signature'>{{@person.signature}}</p>
```

You will be able to use `<PersonProfile />` to invoke this component elsewhere
in your application:

```app/templates/application.hbs
<PersonProfile @person={{this.currentUser}} />
```

Note that component names are capitalized here in order to distinguish them
from regular HTML elements, but they are dasherized in the file system.

While the angle bracket invocation form is generally preferred, it is also
possible to invoke the same component with the `{{person-profile}}` syntax:

```app/templates/application.hbs
{{person-profile person=this.currentUser}}
```

Note that with this syntax, you use dashes in the component name and
arguments are passed without the `@` sign.

In both cases, Ember will render the content of the component template we
created above. The end result will be something like this:

```html
<h1>Tomster</h1>
<img src="https://emberjs.com/tomster.jpg">
<p class='signature'>Out of office this week</p>
```

## File System Nesting

Components can be nested inside sub-folders for logical groupping. For
example, if we placed our template in
`app/templates/components/person/short-profile.hbs`, we can invoke it as
`<Person::ShortProfile />`:

```app/templates/application.hbs
<Person::ShortProfile @person={{this.currentUser}} />
```

Or equivalently, `{{person/short-profile}}`:

```app/templates/application.hbs
{{person/short-profile person=this.currentUser}}
```

## Using Blocks

You can use `yield` inside a template to include the **contents** of any block
attached to the component. For instance, if we added a `{{yield}}` to our
component like so:

```app/templates/components/person-profile.hbs
<h1>{{@person.name}}</h1>
{{yield}}
```

We could then invoke it like this:

```handlebars
<PersonProfile @person={{this.currentUser}}>
<p>Admin mode</p>
</PersonProfile>
```

or with curly syntax like this:

```handlebars
{{#person-profile person=this.currentUser}}
<p>Admin mode</p>
{{/person-profile}}
```

And the content passed in between the brackets of the component would be
rendered in the same place as the `{{yield}}` within it, replacing it.

Blocks are executed in their original context, meaning they have access to the
scope and any in-scope variables where they were defined.

### Passing parameters to blocks

You can also pass positional parameters to `{{yield}}`, which are then made
available in the block:

```app/templates/components/person-profile.hbs
<h1>{{@person.name}}</h1>
{{yield @person.signature}}
```

We can then use this value in the block like so:

```handlebars
<PersonProfile @person={{this.currentUser}} as |signature|>
{{signature}}
</PersonProfile>
```

### Passing multiple blocks

You can pass multiple blocks to a component by giving them names, and
specifying which block you are yielding to with `{{yield}}`. For instance, if
we wanted to add a way for users to customize the title of our
`<PersonProfile>` component, we could add a named block inside of the header:

```app/templates/components/person-profile.hbs
<h1>{{yield to="title"}}</h1>
{{yield}}
```

This component could then be invoked like so:

```handlebars
<PersonProfile @person={{this.currentUser}}>
<:title>{{this.currentUser.name}}</:title>
<:default>{{this.currentUser.signature}}</:default>
</PersonProfile>
```

When passing named blocks, you must name every block, including the `default`
block, which is the block that is defined if you do not pass a `to` parameter
to `{{yield}}`. Whenever you invoke a component without passing explicitly
named blocks, the passed block is considered the `default` block.

### Passing parameters to named blocks

You can also pass parameters to named blocks:

```app/templates/components/person-profile.hbs
<h1>{{yield @person.name to="title"}}</h1>
{{yield @person.signature}}
```

These parameters can then be used like so:

```handlebars
<PersonProfile @person={{this.currentUser}}>
<:title as |name|>{{name}}</:title>
<:default as |signature|>{{signature}}</:default>
</PersonProfile>
```

### Checking to see if a block exists

You can also check to see if a block exists using the `(has-block)` keyword,
and conditionally use it, or provide a default template instead.

```app/templates/components/person-profile.hbs
<h1>
{{#if (has-block "title")}}
{{yield @person.name to="title"}}
{{else}}
{{@person.name}}
{{/if}}
</h1>

{{#if (has-block)}}
{{yield @person.signature}}
{{else}}
{{@person.signature}}
{{/if}}
```

With this template, we can then optionally pass in one block, both blocks, or
none at all:

```handlebars
{{! passing both blocks }}
<PersonProfile @person={{this.currentUser}}>
<:title as |name|>{{name}}</:title>
<:default as |signature|>{{signature}}</:default>
</PersonProfile>

{{! passing just the title block }}
<PersonProfile @person={{this.currentUser}}>
<:title as |name|>{{name}}</:title>
</PersonProfile>

{{! passing just the default block }}
<PersonProfile @person={{this.currentUser}} as |signature|>
{{signature}}
</PersonProfile>

{{! not passing any blocks }}
<PersonProfile @person={{this.currentUser}}/>
```

### Checking to see if a block has parameters

We can also check if a block receives parameters using the `(has-block-params)`
keyword, and conditionally yield different values if so.

```app/templates/components/person-profile.hbs
{{#if (has-block-params)}}
{{yield @person.signature}}
{{else}}
{{yield}}
{{/if}}
```

## Customizing Components With JavaScript

To add JavaScript to a component, create a JavaScript file in the same
location as the template file, with the same name, and export a subclass
of `Component` as the default value. For example, to add Javascript to the
`PersonProfile` component which we defined above, we would create
`app/components/person-profile.js` and export our class as the default, like
so:

```app/components/person-profile.js
import Component from '@glimmer/component';

export default class PersonProfileComponent extends Component {
get displayName() {
let { title, firstName, lastName } = this.args.person;

if (title) {
return `${title} ${lastName}`;
} else {
return `${firstName} ${lastName}`;
}
})
}
```

You can add your own properties, methods, and lifecycle hooks to this
subclass to customize its behavior, and you can reference the instance of the
class in your template using `{{this}}`. For instance, we could access the
`displayName` property of our `PersonProfile` component instance in the
template like this:

```app/templates/components/person-profile.hbs
<h1>{{this.displayName}}</h1>
{{yield}}
```

## `constructor`

params: `owner` object and `args` object

Constructs a new component and assigns itself the passed properties. The
constructor is run whenever a new instance of the component is created, and
can be used to setup the initial state of the component.

```javascript
import Component from '@glimmer/component';

export default class SomeComponent extends Component {
constructor(owner, args) {
super(owner, args);

if (this.args.displayMode === 'list') {
this.items = [];
}
}
}
```

Service injections and arguments are available in the constructor.

```javascript
import Component from '@glimmer/component';
import { service } from '@ember/service';

export default class SomeComponent extends Component {
@service myAnimations;

constructor(owner, args) {
super(owner, args);

if (this.args.fadeIn === true) {
this.myAnimations.register(this, 'fade-in');
}
}
}
```

## `willDestroy`

`willDestroy` is called after the component has been removed from the DOM, but
before the component is fully destroyed. This lifecycle hook can be used to
cleanup the component and any related state.

```javascript
import Component from '@glimmer/component';
import { service } from '@ember/service';

export default class SomeComponent extends Component {
@service myAnimations;

willDestroy() {
super.willDestroy();

this.myAnimations.unregister(this);
}
}
```

## `args`

The `args` property of Glimmer components is an object that contains the
_arguments_ that are passed to the component. For instance, the
following component usage:

```handlebars
<SomeComponent @fadeIn={{true}} />
```

Would result in the following `args` object to be passed to the component:

```javascript
{ fadeIn: true }
```

`args` can be accessed at any point in the component lifecycle, including
`constructor` and `willDestroy`. They are also automatically marked as tracked
properties, and they can be depended on as computed property dependencies:

```javascript
import Component from '@glimmer/component';
import { computed } from '@ember/object';

export default class SomeComponent extends Component {

@computed('args.someValue')
get computedGetter() {
// updates whenever args.someValue updates
return this.args.someValue;
}

get standardGetter() {
// updates whenever args.anotherValue updates (Ember 3.13+)
return this.args.anotherValue;
}
}
```

## `isDestroying`

A boolean flag to tell if the component is in the process of destroying. This is set to
true before `willDestroy` is called.

## `isDestroyed`
A boolean to tell if the component has been fully destroyed. This is set to true
after `willDestroy` is called.

@module @glimmer/component
@public
*/
var Component = class extends GlimmerComponent {
	constructor(owner, args) {
		super(owner, args);
		if (!(owner !== null && typeof owner === "object")) throw new Error(`You must pass both the owner and args to super() in your component: ${this.constructor.name}. You can pass them directly, or use ...arguments to pass all arguments through.`);
		setOwner(this, owner);
	}
};
setComponentManager((owner) => {
	return new EmberGlimmerComponentManager(owner);
}, Component);
//#endregion
//#region node_modules/ember-source/dist/dev/packages/shared-chunks/or-BTH3yuFG.js
var and = internalHelper(({ positional }) => {
	if (positional.length < 2) throw new Error(`\`and\` expects at least two arguments, but received ${positional.length}.`);
	return createComputeRef(() => {
		let last;
		for (let i = 0; i < positional.length; i++) {
			let arg = positional[i];
			last = arg ? valueForRef(arg) : arg;
			if (!toBool(last)) return last;
		}
		return last;
	}, null, "and");
});
/**
* Performs a strict equality comparison.
*
* left === right
*/
function eq(left, right) {
	if (arguments.length !== 2) throw new Error(`\`eq\` expects exactly two arguments, but received ${arguments.length}.`);
	return left === right;
}
/**
* Performs a greater than comparison.
*
* left > right
*/
function gt(left, right) {
	if (arguments.length !== 2) throw new Error(`\`gt\` expects exactly two arguments, but received ${arguments.length}.`);
	return left > right;
}
/**
* Performs a greater than or equal comparison.
*
* left >= right
*/
function gte(left, right) {
	if (arguments.length !== 2) throw new Error(`\`gte\` expects exactly two arguments, but received ${arguments.length}.`);
	return left >= right;
}
/**
* Performs a less than comparison.
*
* left < right
*/
function lt(left, right) {
	if (arguments.length !== 2) throw new Error(`\`lt\` expects exactly two arguments, but received ${arguments.length}.`);
	return left < right;
}
/**
* Performs a less than or equal comparison.
*
* left <= right
*/
function lte(left, right) {
	if (arguments.length !== 2) throw new Error(`\`lte\` expects exactly two arguments, but received ${arguments.length}.`);
	return left <= right;
}
/**
* Performs a strict inequality comparison.
*
* left !== right
*/
function neq(left, right) {
	if (arguments.length !== 2) throw new Error(`\`neq\` expects exactly two arguments, but received ${arguments.length}.`);
	return left !== right;
}
var not = (...args) => {
	if (args.length !== 1) throw new Error(`\`not\` expects exactly one argument, but received ${args.length}.`);
	return !toBool(args[0]);
};
var or = internalHelper(({ positional }) => {
	if (positional.length < 2) throw new Error(`\`or\` expects at least two arguments, but received ${positional.length}.`);
	return createComputeRef(() => {
		let last;
		for (let i = 0; i < positional.length; i++) {
			let arg = positional[i];
			last = arg ? valueForRef(arg) : arg;
			if (toBool(last)) return last;
		}
		return last;
	}, null, "or");
});
//#endregion
//#region node_modules/ember-source/dist/dev/packages/shared-chunks/invoke-B8M5fQQS.js
var ARGS_CACHES = /* @__PURE__ */ new WeakMap();
function getArgs(proxy) {
	return getValue(ARGS_CACHES.get(proxy));
}
var SimpleArgsProxy = class {
	argsCache;
	constructor(context, computeArgs = () => EMPTY_ARGS) {
		let argsCache = createCache(() => computeArgs(context));
		ARGS_CACHES.set(this, argsCache);
		Object.freeze(this);
	}
	get named() {
		return getArgs(this).named || EMPTY_NAMED;
	}
	get positional() {
		return getArgs(this).positional || EMPTY_POSITIONAL;
	}
};
function invokeHelper(context, definition, computeArgs) {
	if (typeof context !== "object" || context === null) throw new Error(`Expected a context object to be passed as the first parameter to invokeHelper, got ${context}`);
	const owner = getOwner(context);
	const internalManager = getInternalHelperManager(definition);
	if (typeof internalManager === "function") throw new Error("Found a helper manager, but it was an internal built-in helper manager. `invokeHelper` does not support internal helpers yet.");
	const manager = internalManager.getDelegateFor(owner);
	let args = new SimpleArgsProxy(context, computeArgs);
	let bucket = manager.createHelper(definition, args);
	let cache;
	if (hasValue(manager)) {
		cache = createCache(() => {
			if (isDestroying(cache) || isDestroyed(cache)) throw new Error(`You attempted to get the value of a helper after the helper was destroyed, which is not allowed`);
			return manager.getValue(bucket);
		});
		associateDestroyableChild(context, cache);
	} else throw new Error("TODO: unreachable, to be implemented with hasScheduledEffect");
	if (hasDestroyable(manager)) {
		let destroyable = manager.getDestroyable(bucket);
		associateDestroyableChild(cache, destroyable);
	}
	return cache;
}
//#endregion
//#region node_modules/ember-source/dist/dev/packages/@glimmer/runtime/index.js
var runtime_exports = /* @__PURE__ */ __exportAll({
	ConcreteBounds: () => ConcreteBounds,
	CurriedValue: () => CurriedValue,
	CursorImpl: () => CursorImpl,
	DOMChanges: () => DOMChanges,
	DOMTreeConstruction: () => DOMTreeConstruction,
	DynamicAttribute: () => DynamicAttribute,
	DynamicScopeImpl: () => DynamicScopeImpl,
	EMPTY_ARGS: () => EMPTY_ARGS,
	EMPTY_NAMED: () => EMPTY_NAMED,
	EMPTY_POSITIONAL: () => EMPTY_POSITIONAL,
	EnvironmentImpl: () => EnvironmentImpl,
	IDOMChanges: () => DOMChangesImpl,
	LowLevelVM: () => LowLevelVM,
	NewTreeBuilder: () => NewTreeBuilder,
	RehydrateTree: () => RehydrateTree,
	RemoteBlock: () => RemoteBlock,
	ResettableBlockImpl: () => ResettableBlockImpl,
	SERIALIZATION_FIRST_NODE_STRING: () => SERIALIZATION_FIRST_NODE_STRING$1,
	ScopeImpl: () => ScopeImpl,
	SimpleDynamicAttribute: () => SimpleDynamicAttribute,
	TEMPLATE_ONLY_COMPONENT_MANAGER: () => TEMPLATE_ONLY_COMPONENT_MANAGER,
	TemplateOnlyComponent: () => TemplateOnlyComponentDefinition,
	TemplateOnlyComponentManager: () => TemplateOnlyComponentManager,
	UpdatingVM: () => UpdatingVM,
	and: () => and,
	array: () => array,
	clear: () => clear,
	clientBuilder: () => clientBuilder,
	concat: () => concat,
	createCapturedArgs: () => createCapturedArgs,
	curry: () => curry,
	destroy: () => destroy,
	dynamicAttribute: () => dynamicAttribute,
	eq: () => eq,
	fn: () => fn,
	get: () => get,
	gt: () => gt,
	gte: () => gte,
	hash: () => hash$1,
	inTransaction: () => inTransaction,
	invokeHelper: () => invokeHelper,
	isDestroyed: () => isDestroyed,
	isDestroying: () => isDestroying,
	isSerializationFirstNode: () => isSerializationFirstNode$1,
	isWhitespace: () => isWhitespace,
	lt: () => lt,
	lte: () => lte,
	neq: () => neq,
	normalizeProperty: () => normalizeProperty,
	not: () => not,
	on: () => on$1,
	or: () => or,
	registerDestructor: () => registerDestructor,
	rehydrationBuilder: () => rehydrationBuilder,
	reifyArgs: () => reifyArgs,
	reifyNamed: () => reifyNamed,
	reifyPositional: () => reifyPositional,
	renderComponent: () => renderComponent,
	renderMain: () => renderMain,
	renderSync: () => renderSync,
	resetDebuggerCallback: () => resetDebuggerCallback,
	runtimeOptions: () => runtimeOptions,
	setDebuggerCallback: () => setDebuggerCallback,
	templateOnlyComponent: () => templateOnlyComponent
});
//#endregion
//#region node_modules/ember-source/dist/dev/packages/@glimmer/util/index.js
var util_exports = /* @__PURE__ */ __exportAll({
	EMPTY_ARRAY: () => EMPTY_ARRAY,
	EMPTY_NUMBER_ARRAY: () => EMPTY_NUMBER_ARRAY,
	EMPTY_STRING_ARRAY: () => EMPTY_STRING_ARRAY,
	LOCAL_LOGGER: () => LOCAL_LOGGER,
	LOGGER: () => LOGGER,
	SERIALIZATION_FIRST_NODE_STRING: () => SERIALIZATION_FIRST_NODE_STRING,
	Stack: () => StackImpl,
	assertNever: () => assertNever,
	assign: () => assign,
	beginTestSteps: () => beginTestSteps,
	clearElement: () => clearElement,
	dict: () => dict,
	emptyArray: () => emptyArray,
	endTestSteps: () => endTestSteps,
	entries: () => entries,
	enumerate: () => enumerate,
	intern: () => intern,
	isDict: () => isDict,
	isEmptyArray: () => isEmptyArray,
	isIndexable: () => isIndexable,
	isSerializationFirstNode: () => isSerializationFirstNode,
	keys: () => keys,
	logStep: () => logStep,
	reverse: () => reverse,
	strip: () => strip,
	values: () => values,
	verifySteps: () => verifySteps,
	zipArrays: () => zipArrays,
	zipTuples: () => zipTuples
});
/**
* This constant exists to make it easier to differentiate normal logs from
* errant console.logs. LOCAL_LOGGER should only be used inside a
* LOCAL_TRACE_LOGGING check.
*
* It does not alleviate the need to check LOCAL_TRACE_LOGGING, which is used
* for stripping.
*/
var LOCAL_LOGGER = console;
/**
* This constant exists to make it easier to differentiate normal logs from
* errant console.logs. LOGGER can be used outside of LOCAL_TRACE_LOGGING checks,
* and is meant to be used in the rare situation where a console.* call is
* actually appropriate.
*/
var LOGGER = console;
var beginTestSteps;
var endTestSteps;
var verifySteps;
var logStep;
function clearElement(parent) {
	let current = parent.firstChild;
	while (current) {
		let next = current.nextSibling;
		parent.removeChild(current);
		current = next;
	}
}
/**
Strongly hint runtimes to intern the provided string.

When do I need to use this function?

For the most part, never. Pre-mature optimization is bad, and often the
runtime does exactly what you need it to, and more often the trade-off isn't
worth it.

Why?

Runtimes store strings in at least 2 different representations:
Ropes and Symbols (interned strings). The Rope provides a memory efficient
data-structure for strings created from concatenation or some other string
manipulation like splitting.

Unfortunately checking equality of different ropes can be quite costly as
runtimes must resort to clever string comparison algorithms. These
algorithms typically cost in proportion to the length of the string.
Luckily, this is where the Symbols (interned strings) shine. As Symbols are
unique by their string content, equality checks can be done by pointer
comparison.

How do I know if my string is a rope or symbol?

Typically (warning general sweeping statement, but truthy in runtimes at
present) static strings created as part of the JS source are interned.
Strings often used for comparisons can be interned at runtime if some
criteria are met.  One of these criteria can be the size of the entire rope.
For example, in chrome 38 a rope longer then 12 characters will not
intern, nor will segments of that rope.

Some numbers: http://jsperf.com/eval-vs-keys/8

Known Trick™

@private
@return {String} interned version of the provided string
*/
function intern(str) {
	let obj = {};
	obj[str] = 1;
	for (let key in obj) if (key === str) return key;
	return str;
}
var SERIALIZATION_FIRST_NODE_STRING = "%+b:0%";
function isSerializationFirstNode(node) {
	return node.nodeValue === SERIALIZATION_FIRST_NODE_STRING;
}
function strip(strings, ...args) {
	let out = "";
	for (const [i, string] of enumerate(strings)) {
		let dynamic = args[i] !== void 0 ? String(args[i]) : "";
		out += `${string}${dynamic}`;
	}
	let lines = out.split("\n");
	while (isPresentArray(lines) && /^\s*$/u.test(getFirst(lines))) lines.shift();
	while (isPresentArray(lines) && /^\s*$/u.test(getLast(lines))) lines.pop();
	let min = Infinity;
	for (let line of lines) {
		let leading = /^\s*/u.exec(line)[0].length;
		min = Math.min(min, leading);
	}
	let stripped = [];
	for (let line of lines) stripped.push(line.slice(min));
	return stripped.join("\n");
}
function assertNever(value, desc = "unexpected unreachable branch") {
	LOGGER.log("unreachable", value);
	LOGGER.log(`${desc} :: ${JSON.stringify(value)} (${value})`);
	throw new Error(`code reached unreachable`);
}
//#endregion
export { application_exports as Application, namespace_exports as ApplicationNamespace, array_exports as Array, mutable_exports as ArrayMutable, proxy_exports as ArrayProxy, component_exports as Component, controller_exports as Controller, debug_exports as Debug, destroyable_exports as EmberDestroyable, object_exports as EmberObject, object_exports as Object, mutable_exports$1 as EnumerableMutable, dist_exports as GlimmerComponent, manager_exports as GlimmerManager, reference_exports as GlimmerReference, runtime_exports as GlimmerRuntime, util_exports as GlimmerUtil, validator_exports as GlimmerValidator, instrumentation_exports as Instrumentation, environment_exports as InternalsEnvironment, meta_exports as InternalsMeta, metal_exports as InternalsMetal, runtime_exports$1 as InternalsRuntime, utils_exports as InternalsUtils, views_exports as InternalsViews, core_exports as ObjectCore, evented_exports as ObjectEvented, internals_exports as ObjectInternals, observable_exports as ObjectObservable, promise_proxy_mixin_exports as ObjectPromiseProxyMixin, proxy_exports$1 as ObjectProxy, owner_exports as Owner, rsvp_exports as RSVP, runloop_exports as Runloop, service_exports as Service, version_exports as VERSION };
