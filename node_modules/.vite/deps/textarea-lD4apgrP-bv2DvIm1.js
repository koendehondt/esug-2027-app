import { b as setViewElement, c as clearViewElement, d as getElementView, g as getViewElement, n as opaquify, o as addChildView, r as _instrumentStart, s as clearElementView, t as InternalComponent, y as setElementView } from "./internal-CAL-jHZt-Dyqb4Z6W.js";
import { C as getOwner, T as setOwner, bt as get, i as set } from "./set_properties-BghuuGJx-C1lXKl_Y.js";
import { C as hasDOM, f as warn, r as debugFreeze, y as assert } from "./debug-CzKtnQbf.js";
import { $ as valueForTag, H as endTrackFrame, L as consumeTag, N as beginTrackFrame, P as beginUntrackFrame, Q as validateTag, S as valueForRef, U as endUntrackFrame, X as untrack, b as isUpdatableRef, ct as registerDestructor, d as createComputeRef, f as createConstRef, h as createPrimitiveRef, l as childRefFor, u as childRefFromParts, v as isConstRef, x as updateRef } from "./args-proxy-BNLuRFlO-EccNNvt4.js";
import { n as action, p as guidFor, t as EmberObject } from "./object-CjD7BznD.js";
import { n as decorateMethodV2, r as initializeDeferredDecorator, t as decorateFieldV2 } from "./runtime-CYyqkz5q-BOdRhmsS-CnoKbWmq.js";
import { l as EMPTY_ARRAY } from "./assert-Zqc4wiAV-CoU6R4iL.js";
import { C as reifyPositional, t as on } from "./on-SjUz-B1Q.js";
import { n as dasherize } from "./string-BDkcsqzT.js";
import { c as templateFactory } from "./index-CyZiGiEI-Cq3SnK-P.js";
import { n as tracked } from "./tracked-CKE4wnE6-EulZSk-Y.js";
//#region node_modules/ember-source/dist/dev/packages/@ember/-internals/views/lib/system/event_dispatcher.js
/**
@module ember
*/
var ROOT_ELEMENT_CLASS = "ember-application";
var ROOT_ELEMENT_SELECTOR = `.${ROOT_ELEMENT_CLASS}`;
/**
`EventDispatcher` handles delegating browser events to their
corresponding `Ember.Views.` For example, when you click on a view,
`EventDispatcher` ensures that that view's `mouseDown` method gets
called.

@class EventDispatcher
@namespace Ember
@private
@extends EmberObject
*/
var EventDispatcher = class extends EmberObject {
	/**
	The set of events names (and associated handler function names) to be setup
	and dispatched by the `EventDispatcher`. Modifications to this list can be done
	at setup time, generally via the `Application.customEvents` hash.
	To add new events to be listened to:
	```javascript
	import Application from '@ember/application';
	let App = Application.create({
	customEvents: {
	paste: 'paste'
	}
	});
	```
	To prevent default events from being listened to:
	```javascript
	import Application from '@ember/application';
	let App = Application.create({
	customEvents: {
	mouseenter: null,
	mouseleave: null
	}
	});
	```
	@property events
	@type Object
	@private
	*/
	events = {
		touchstart: "touchStart",
		touchmove: "touchMove",
		touchend: "touchEnd",
		touchcancel: "touchCancel",
		keydown: "keyDown",
		keyup: "keyUp",
		keypress: "keyPress",
		mousedown: "mouseDown",
		mouseup: "mouseUp",
		contextmenu: "contextMenu",
		click: "click",
		dblclick: "doubleClick",
		focusin: "focusIn",
		focusout: "focusOut",
		submit: "submit",
		input: "input",
		change: "change",
		dragstart: "dragStart",
		drag: "drag",
		dragenter: "dragEnter",
		dragleave: "dragLeave",
		dragover: "dragOver",
		drop: "drop",
		dragend: "dragEnd"
	};
	/**
	The root DOM element to which event listeners should be attached. Event
	listeners will be attached to the document unless this is overridden.
	Can be specified as a DOMElement or a selector string.
	The default body is a string since this may be evaluated before document.body
	exists in the DOM.
	@private
	@property rootElement
	@type DOMElement
	@default 'body'
	*/
	rootElement = "body";
	_eventHandlers = Object.create(null);
	_didSetup = false;
	finalEventNameMapping = null;
	_sanitizedRootElement = null;
	lazyEvents = /* @__PURE__ */ new Map();
	_reverseEventNameMapping = null;
	/**
	Sets up event listeners for standard browser events.
	This will be called after the browser sends a `DOMContentReady` event. By
	default, it will set up all of the listeners on the document body. If you
	would like to register the listeners on a different element, set the event
	dispatcher's `root` property.
	@private
	@method setup
	@param addedEvents {Object}
	*/
	setup(addedEvents, _rootElement) {
		!(() => {
			let owner = getOwner(this);
			!owner && assert("[BUG] Missing owner", owner);
			return owner.lookup("-environment:main").isInteractive;
		})() && assert("EventDispatcher should never be setup in fastboot mode. Please report this as an Ember bug.", (() => {
			let owner = getOwner(this);
			!owner && assert("[BUG] Missing owner", owner);
			return owner.lookup("-environment:main").isInteractive;
		})());
		let events = this.finalEventNameMapping = {
			...get(this, "events"),
			...addedEvents
		};
		this._reverseEventNameMapping = Object.keys(events).reduce((result, key) => {
			let eventName = events[key];
			return eventName ? {
				...result,
				[eventName]: key
			} : result;
		}, {});
		let lazyEvents = this.lazyEvents;
		if (_rootElement !== void 0 && _rootElement !== null) set(this, "rootElement", _rootElement);
		let specifiedRootElement = get(this, "rootElement");
		let rootElement = typeof specifiedRootElement !== "string" ? specifiedRootElement : document.querySelector(specifiedRootElement);
		!rootElement && assert(`Could not find rootElement (${specifiedRootElement})`, rootElement);
		rootElement.classList.contains(ROOT_ELEMENT_CLASS) && assert(`You cannot use the same root element (${specifiedRootElement}) multiple times in an Ember.Application`, !rootElement.classList.contains(ROOT_ELEMENT_CLASS));
		!(() => {
			let target = rootElement.parentNode;
			while (target instanceof Element) {
				if (target.classList.contains(ROOT_ELEMENT_CLASS)) return false;
				target = target.parentNode;
			}
			return true;
		})() && assert("You cannot make a new Ember.Application using a root element that is a descendent of an existing Ember.Application", (() => {
			let target = rootElement.parentNode;
			while (target instanceof Element) {
				if (target.classList.contains(ROOT_ELEMENT_CLASS)) return false;
				target = target.parentNode;
			}
			return true;
		})());
		rootElement.querySelector(ROOT_ELEMENT_SELECTOR) && assert("You cannot make a new Ember.Application using a root element that is an ancestor of an existing Ember.Application", !rootElement.querySelector(ROOT_ELEMENT_SELECTOR));
		rootElement.classList.add(ROOT_ELEMENT_CLASS);
		!rootElement.classList.contains(ROOT_ELEMENT_CLASS) && assert(`Unable to add '${ROOT_ELEMENT_CLASS}' class to root element (${get(this, "rootElement") || rootElement.tagName}). Make sure you set rootElement to the body or an element in the body.`, rootElement.classList.contains(ROOT_ELEMENT_CLASS));
		this._sanitizedRootElement = rootElement;
		for (let event in events) if (Object.prototype.hasOwnProperty.call(events, event)) lazyEvents.set(event, events[event] ?? null);
		this._didSetup = true;
	}
	/**
	Setup event listeners for the given browser event name
	@private
	@method setupHandlerForBrowserEvent
	@param event the name of the event in the browser
	*/
	setupHandlerForBrowserEvent(event) {
		!this.finalEventNameMapping && assert("[BUG] Expected finalEventNameMapping to be set", this.finalEventNameMapping);
		!this._sanitizedRootElement && assert("[BUG] Expected _santizedRootElement to be set", this._sanitizedRootElement);
		this.setupHandler(this._sanitizedRootElement, event, this.finalEventNameMapping[event] ?? null);
	}
	/**
	Setup event listeners for the given Ember event name (camel case)
	@private
	@method setupHandlerForEmberEvent
	@param eventName
	*/
	setupHandlerForEmberEvent(eventName) {
		!this._sanitizedRootElement && assert("[BUG] Expected _sanitizedRootElement to be set", this._sanitizedRootElement);
		let event = this._reverseEventNameMapping?.[eventName];
		if (event) this.setupHandler(this._sanitizedRootElement, event, eventName);
	}
	/**
	Registers an event listener on the rootElement. If the given event is
	triggered, the provided event handler will be triggered on the target view.
	If the target view does not implement the event handler, or if the handler
	returns `false`, the parent view will be called. The event will continue to
	bubble to each successive parent view until it reaches the top.
	@private
	@method setupHandler
	@param {Element} rootElement
	@param {String} event the name of the event in the browser
	@param {String} eventName the name of the method to call on the view
	*/
	setupHandler(rootElement, event, eventName) {
		if (eventName === null || !this.lazyEvents.has(event)) return;
		let viewHandler = (target, event) => {
			let view = getElementView(target);
			let result = true;
			if (view) result = view.handleEvent(eventName, event);
			return result;
		};
		let handleEvent = this._eventHandlers[event] = (event) => {
			let target = event.target;
			!(target instanceof Element) && assert(`[BUG] Received event without an Element target: ${event.type}, ${target}`, target instanceof Element);
			do {
				if (getElementView(target)) {
					if (viewHandler(target, event) === false) {
						event.preventDefault();
						event.stopPropagation();
						break;
					} else if (event.cancelBubble === true) break;
				}
				target = target.parentNode;
			} while (target instanceof Element);
		};
		rootElement.addEventListener(event, handleEvent);
		this.lazyEvents.delete(event);
	}
	destroy() {
		if (this._didSetup === false) return;
		let rootElement = this._sanitizedRootElement;
		if (!rootElement) return;
		for (let event in this._eventHandlers) rootElement.removeEventListener(event, this._eventHandlers[event]);
		rootElement.classList.remove(ROOT_ELEMENT_CLASS);
		return this._super(...arguments);
	}
	toString() {
		return "(EventDispatcher)";
	}
};
//#endregion
//#region node_modules/ember-source/dist/dev/packages/shared-chunks/props-fiqxqhAH.js
function normalizeProperty(element, slotName) {
	let type, normalized;
	if (slotName in element) {
		normalized = slotName;
		type = "prop";
	} else {
		let lower = slotName.toLowerCase();
		if (lower in element) {
			type = "prop";
			normalized = lower;
		} else {
			type = "attr";
			normalized = slotName;
		}
	}
	if (type === "prop" && (normalized.toLowerCase() === "style" || preferAttr(element.tagName, normalized))) type = "attr";
	return {
		normalized,
		type
	};
}
var ATTR_OVERRIDES = {
	INPUT: {
		form: true,
		autocorrect: true,
		list: true
	},
	SELECT: { form: true },
	OPTION: { form: true },
	TEXTAREA: { form: true },
	LABEL: { form: true },
	FIELDSET: { form: true },
	LEGEND: { form: true },
	OBJECT: { form: true },
	OUTPUT: { form: true },
	BUTTON: { form: true }
};
function preferAttr(tagName, propName) {
	let tag = ATTR_OVERRIDES[tagName.toUpperCase()];
	return !!(tag && tag[propName.toLowerCase()]);
}
//#endregion
//#region node_modules/ember-source/dist/dev/packages/@ember/-internals/views/lib/compat/attrs.js
var MUTABLE_CELL = Symbol("MUTABLE_CELL");
//#endregion
//#region node_modules/ember-source/dist/dev/packages/shared-chunks/curly-BBzF_bI8.js
/**
* @deprecated
*/
function unwrapTemplate(template) {
	if (template.result === "error") throw new Error(`Compile Error: ${template.problem} @ ${template.span.start}..${template.span.end}`);
	return template;
}
function isTemplateFactory(template) {
	return typeof template === "function";
}
function referenceForParts(rootRef, parts) {
	if (parts[0] === "attrs") {
		parts.shift();
		if (parts.length === 1) return childRefFor(rootRef, parts[0]);
	}
	return childRefFromParts(rootRef, parts);
}
function parseAttributeBinding(microsyntax) {
	let colonIndex = microsyntax.indexOf(":");
	if (colonIndex === -1) {
		!(microsyntax !== "class") && assert("You cannot use class as an attributeBinding, use classNameBindings instead.", microsyntax !== "class");
		return [
			microsyntax,
			microsyntax,
			true
		];
	} else {
		let prop = microsyntax.substring(0, colonIndex);
		let attribute = microsyntax.substring(colonIndex + 1);
		!(attribute !== "class") && assert("You cannot use class as an attributeBinding, use classNameBindings instead.", attribute !== "class");
		return [
			prop,
			attribute,
			false
		];
	}
}
function installAttributeBinding(component, rootRef, parsed, operations) {
	let [prop, attribute, isSimple] = parsed;
	if (attribute === "id") {
		let elementId = get(component, prop);
		if (elementId === void 0 || elementId === null) elementId = component.elementId;
		let elementIdRef = createPrimitiveRef(elementId);
		operations.setAttribute("id", elementIdRef, true, null);
		return;
	}
	let isPath = prop.indexOf(".") > -1;
	let reference = isPath ? referenceForParts(rootRef, prop.split(".")) : childRefFor(rootRef, prop);
	isSimple && isPath && assert(`Illegal attributeBinding: '${prop}' is not a valid attribute name.`, !(isSimple && isPath));
	operations.setAttribute(attribute, reference, false, null);
}
function createClassNameBindingRef(rootRef, microsyntax, operations) {
	let [prop, truthy, falsy] = microsyntax.split(":");
	!(prop !== void 0) && assert("has prop", prop !== void 0);
	if (prop === "") operations.setAttribute("class", createPrimitiveRef(truthy), true, null);
	else {
		let isPath = prop.indexOf(".") > -1;
		let parts = isPath ? prop.split(".") : [];
		let value = isPath ? referenceForParts(rootRef, parts) : childRefFor(rootRef, prop);
		let ref;
		if (truthy === void 0) ref = createSimpleClassNameBindingRef(value, isPath ? parts[parts.length - 1] : prop);
		else ref = createColonClassNameBindingRef(value, truthy, falsy);
		operations.setAttribute("class", ref, false, null);
	}
}
function createSimpleClassNameBindingRef(inner, path) {
	let dasherizedPath;
	return createComputeRef(() => {
		let value = valueForRef(inner);
		if (value === true) {
			!(path !== void 0) && assert("You must pass a path when binding a to a class name using classNameBindings", path !== void 0);
			return dasherizedPath || (dasherizedPath = dasherize(path));
		} else if (value || value === 0) return String(value);
		else return null;
	});
}
function createColonClassNameBindingRef(inner, truthy, falsy) {
	return createComputeRef(() => {
		return valueForRef(inner) ? truthy : falsy;
	});
}
function NOOP() {}
/**
@module ember
*/
/**
Represents the internal state of the component.

@class ComponentStateBucket
@private
*/
var ComponentStateBucket = class {
	classRef = null;
	rootRef;
	argsRevision;
	constructor(component, args, argsTag, finalizer, hasWrappedElement, isInteractive) {
		this.component = component;
		this.args = args;
		this.argsTag = argsTag;
		this.finalizer = finalizer;
		this.hasWrappedElement = hasWrappedElement;
		this.isInteractive = isInteractive;
		this.classRef = null;
		this.argsRevision = args === null ? 0 : valueForTag(argsTag);
		this.rootRef = createConstRef(component, "this");
		registerDestructor(this, () => this.willDestroy(), true);
		registerDestructor(this, () => this.component.destroy());
	}
	willDestroy() {
		let { component, isInteractive } = this;
		if (isInteractive) {
			beginUntrackFrame();
			component.trigger("willDestroyElement");
			component.trigger("willClearRender");
			endUntrackFrame();
			let element = getViewElement(component);
			if (element) {
				clearElementView(element);
				clearViewElement(component);
			}
		}
		component.renderer.unregister(component);
	}
	finalize() {
		let { finalizer } = this;
		finalizer();
		this.finalizer = NOOP;
	}
};
function processComponentArgs(namedArgs) {
	let attrs = Object.create(null);
	let props = Object.create(null);
	for (let name in namedArgs) {
		let ref = namedArgs[name];
		!ref && assert("expected ref", ref);
		let value = valueForRef(ref);
		if (isUpdatableRef(ref)) attrs[name] = new MutableCell(ref, value);
		else attrs[name] = value;
		props[name] = value;
	}
	props.attrs = attrs;
	return props;
}
var REF = Symbol("REF");
var MutableCell = class {
	value;
	[MUTABLE_CELL];
	[REF];
	constructor(ref, value) {
		this[MUTABLE_CELL] = true;
		this[REF] = ref;
		this.value = value;
	}
	update(val) {
		updateRef(this[REF], val);
	}
};
var COMPONENT_ARGS_MAP = /* @__PURE__ */ new WeakMap();
function getComponentCapturedArgs(component) {
	return COMPONENT_ARGS_MAP.get(component);
}
var DIRTY_TAG = Symbol("DIRTY_TAG");
var IS_DISPATCHING_ATTRS = Symbol("IS_DISPATCHING_ATTRS");
var BOUNDS = Symbol("BOUNDS");
var EMBER_VIEW_REF = createPrimitiveRef("ember-view");
function aliasIdToElementId(args, props) {
	if (args.named.has("id")) {
		args.named.has("elementId") && assert(`You cannot invoke a component with both 'id' and 'elementId' at the same time.`, !args.named.has("elementId"));
		props.elementId = props.id;
	}
}
function applyAttributeBindings(attributeBindings, component, rootRef, operations) {
	let seen = [];
	let i = attributeBindings.length - 1;
	while (i !== -1) {
		let binding = attributeBindings[i];
		!binding && assert("has binding", binding);
		let parsed = parseAttributeBinding(binding);
		let attribute = parsed[1];
		if (seen.indexOf(attribute) === -1) {
			seen.push(attribute);
			installAttributeBinding(component, rootRef, parsed, operations);
		}
		i--;
	}
	if (seen.indexOf("id") === -1) {
		let id = component.elementId ? component.elementId : guidFor(component);
		operations.setAttribute("id", createPrimitiveRef(id), false, null);
	}
}
debugFreeze([]);
var CurlyComponentManager = class {
	templateFor(component) {
		let { layout, layoutName } = component;
		let owner = getOwner(component);
		!owner && assert("Component is unexpectedly missing an owner", owner);
		let factory;
		if (layout === void 0) {
			if (layoutName !== void 0) {
				let _factory = owner.lookup(`template:${layoutName}`);
				!(_factory !== void 0) && assert(`Layout \`${layoutName}\` not found!`, _factory !== void 0);
				factory = _factory;
			} else return null;
		} else if (isTemplateFactory(layout)) factory = layout;
		else return null;
		return unwrapTemplate(factory(owner)).asWrappedLayout();
	}
	getDynamicLayout(bucket) {
		return this.templateFor(bucket.component);
	}
	getTagName(state) {
		let { component, hasWrappedElement } = state;
		if (!hasWrappedElement) return null;
		return component && component.tagName || "div";
	}
	getCapabilities() {
		return CURLY_CAPABILITIES;
	}
	prepareArgs(ComponentClass, args) {
		if (args.named.has("__ARGS__")) {
			!(args.positional.length === 0) && assert("[BUG] cannot pass both __ARGS__ and positional arguments", args.positional.length === 0);
			let { __ARGS__, ...rest } = args.named.capture();
			!__ARGS__ && assert("[BUG] unexpectedly missing __ARGS__ after check", __ARGS__);
			let __args__ = valueForRef(__ARGS__);
			return {
				positional: __args__.positional,
				named: {
					...rest,
					...__args__.named
				}
			};
		}
		const { positionalParams } = ComponentClass.class ?? ComponentClass;
		if (positionalParams === void 0 || positionalParams === null || args.positional.length === 0) return null;
		let named;
		if (typeof positionalParams === "string") {
			args.named.has(positionalParams) && assert(`You cannot specify positional parameters and the hash argument \`${positionalParams}\`.`, !args.named.has(positionalParams));
			let captured = args.positional.capture();
			named = { [positionalParams]: createComputeRef(() => reifyPositional(captured)) };
			Object.assign(named, args.named.capture());
		} else if (Array.isArray(positionalParams) && positionalParams.length > 0) {
			const count = Math.min(positionalParams.length, args.positional.length);
			named = {};
			Object.assign(named, args.named.capture());
			for (let i = 0; i < count; i++) {
				let name = positionalParams[i];
				!name && assert("Expected at least one positional param", name);
				args.named.has(name) && assert(`You cannot specify both a positional param (at position ${i}) and the hash argument \`${name}\`.`, !args.named.has(name));
				named[name] = args.positional.at(i);
			}
		} else return null;
		return {
			positional: EMPTY_ARRAY,
			named
		};
	}
	create(owner, ComponentClass, args, { isInteractive }, dynamicScope, callerSelfRef) {
		let parentView = dynamicScope.view;
		let capturedArgs = args.named.capture();
		beginTrackFrame();
		let props = processComponentArgs(capturedArgs);
		let argsTag = endTrackFrame();
		aliasIdToElementId(args, props);
		props.parentView = parentView;
		props._target = valueForRef(callerSelfRef);
		setOwner(props, owner);
		beginUntrackFrame();
		let component = ComponentClass.create(props);
		COMPONENT_ARGS_MAP.set(component, capturedArgs);
		let finalizer = _instrumentStart("render.component", initialRenderInstrumentDetails, component);
		dynamicScope.view = component;
		if (parentView !== null && parentView !== void 0) addChildView(parentView, component);
		component.trigger("didReceiveAttrs");
		let hasWrappedElement = component.tagName !== "";
		if (!hasWrappedElement) {
			if (isInteractive) component.trigger("willRender");
			component._transitionTo("hasElement");
			if (isInteractive) component.trigger("willInsertElement");
		}
		let bucket = new ComponentStateBucket(component, capturedArgs, argsTag, finalizer, hasWrappedElement, isInteractive);
		if (args.named.has("class")) bucket.classRef = args.named.get("class");
		processComponentInitializationAssertions(component, props);
		if (isInteractive && hasWrappedElement) component.trigger("willRender");
		endUntrackFrame();
		consumeTag(bucket.argsTag);
		consumeTag(component[DIRTY_TAG]);
		return bucket;
	}
	getDebugName(definition) {
		return definition.fullName || definition.normalizedName || definition.class?.name || definition.name;
	}
	getSelf({ rootRef }) {
		return rootRef;
	}
	didCreateElement({ component, classRef, isInteractive, rootRef }, element, operations) {
		setViewElement(component, element);
		setElementView(element, component);
		let { attributeBindings, classNames, classNameBindings } = component;
		if (attributeBindings && attributeBindings.length) applyAttributeBindings(attributeBindings, component, rootRef, operations);
		else {
			let id = component.elementId ? component.elementId : guidFor(component);
			operations.setAttribute("id", createPrimitiveRef(id), false, null);
		}
		if (classRef) {
			const ref = createSimpleClassNameBindingRef(classRef);
			operations.setAttribute("class", ref, false, null);
		}
		if (classNames && classNames.length) classNames.forEach((name) => {
			operations.setAttribute("class", createPrimitiveRef(name), false, null);
		});
		if (classNameBindings && classNameBindings.length) classNameBindings.forEach((binding) => {
			createClassNameBindingRef(rootRef, binding, operations);
		});
		operations.setAttribute("class", EMBER_VIEW_REF, false, null);
		if ("ariaRole" in component) operations.setAttribute("role", childRefFor(rootRef, "ariaRole"), false, null);
		component._transitionTo("hasElement");
		if (isInteractive) {
			beginUntrackFrame();
			component.trigger("willInsertElement");
			endUntrackFrame();
		}
	}
	didRenderLayout(bucket, bounds) {
		bucket.component[BOUNDS] = bounds;
		bucket.finalize();
	}
	didCreate({ component, isInteractive }) {
		if (isInteractive) {
			component._transitionTo("inDOM");
			component.trigger("didInsertElement");
			component.trigger("didRender");
		}
	}
	update(bucket) {
		let { component, args, argsTag, argsRevision, isInteractive } = bucket;
		bucket.finalizer = _instrumentStart("render.component", rerenderInstrumentDetails, component);
		beginUntrackFrame();
		if (args !== null && !validateTag(argsTag, argsRevision)) {
			beginTrackFrame();
			let props = processComponentArgs(args);
			argsTag = bucket.argsTag = endTrackFrame();
			bucket.argsRevision = valueForTag(argsTag);
			component[IS_DISPATCHING_ATTRS] = true;
			component.setProperties(props);
			component[IS_DISPATCHING_ATTRS] = false;
			component.trigger("didUpdateAttrs");
			component.trigger("didReceiveAttrs");
		}
		if (isInteractive) {
			component.trigger("willUpdate");
			component.trigger("willRender");
		}
		endUntrackFrame();
		consumeTag(argsTag);
		consumeTag(component[DIRTY_TAG]);
	}
	didUpdateLayout(bucket) {
		bucket.finalize();
	}
	didUpdate({ component, isInteractive }) {
		if (isInteractive) {
			component.trigger("didUpdate");
			component.trigger("didRender");
		}
	}
	getDestroyable(bucket) {
		return bucket;
	}
};
function processComponentInitializationAssertions(component, props) {
	!(() => {
		let { classNameBindings } = component;
		for (let i = 0; i < classNameBindings.length; i++) {
			let binding = classNameBindings[i];
			if (typeof binding !== "string" || binding.length === 0) return false;
		}
		return true;
	})() && assert(`classNameBindings must be non-empty strings: ${component}`, (() => {
		let { classNameBindings } = component;
		for (let i = 0; i < classNameBindings.length; i++) {
			let binding = classNameBindings[i];
			if (typeof binding !== "string" || binding.length === 0) return false;
		}
		return true;
	})());
	!(() => {
		let { classNameBindings } = component;
		for (let binding of classNameBindings) if (binding.split(" ").length > 1) return false;
		return true;
	})() && assert(`classNameBindings must not have spaces in them: ${component}`, (() => {
		let { classNameBindings } = component;
		for (let binding of classNameBindings) if (binding.split(" ").length > 1) return false;
		return true;
	})());
	!(component.tagName !== "" || !component.classNameBindings || component.classNameBindings.length === 0) && assert(`You cannot use \`classNameBindings\` on a tag-less component: ${component}`, component.tagName !== "" || !component.classNameBindings || component.classNameBindings.length === 0);
	!(component.tagName !== "" || props.id === component.elementId || !component.elementId && component.elementId !== "") && assert(`You cannot use \`elementId\` on a tag-less component: ${component}`, component.tagName !== "" || props.id === component.elementId || !component.elementId && component.elementId !== "");
	!(component.tagName !== "" || !component.attributeBindings || component.attributeBindings.length === 0) && assert(`You cannot use \`attributeBindings\` on a tag-less component: ${component}`, component.tagName !== "" || !component.attributeBindings || component.attributeBindings.length === 0);
}
function initialRenderInstrumentDetails(component) {
	return component.instrumentDetails({ initialRender: true });
}
function rerenderInstrumentDetails(component) {
	return component.instrumentDetails({ initialRender: false });
}
var CURLY_CAPABILITIES = {
	dynamicLayout: true,
	dynamicTag: true,
	prepareArgs: true,
	createArgs: true,
	attributeHook: true,
	elementHook: true,
	createCaller: true,
	dynamicScope: true,
	updateHook: true,
	createInstance: true,
	wrapped: true,
	willDestroy: true,
	hasSubOwner: false
};
var CURLY_COMPONENT_MANAGER = new CurlyComponentManager();
function isCurlyManager(manager) {
	return manager === CURLY_COMPONENT_MANAGER;
}
//#endregion
//#region node_modules/ember-source/dist/dev/packages/shared-chunks/textarea-lD4apgrP.js
var InputTemplate = templateFactory({
	"id": null,
	"block": "[[[11,\"input\"],[16,1,[30,0,[\"id\"]]],[16,0,[30,0,[\"class\"]]],[17,1],[16,4,[30,0,[\"type\"]]],[16,\"checked\",[30,0,[\"checked\"]]],[16,2,[30,0,[\"value\"]]],[4,[32,0],[\"change\",[30,0,[\"change\"]]],null],[4,[32,0],[\"input\",[30,0,[\"input\"]]],null],[4,[32,0],[\"keyup\",[30,0,[\"keyUp\"]]],null],[4,[32,0],[\"paste\",[30,0,[\"valueDidChange\"]]],null],[4,[32,0],[\"cut\",[30,0,[\"valueDidChange\"]]],null],[12],[13]],[\"&attrs\"],[]]",
	"moduleName": "packages/@ember/-internals/glimmer/lib/templates/input.hbs",
	"scope": () => ({ on }),
	"isStrictMode": true
});
var UNINITIALIZED = Object.freeze({});
function elementForEvent(event) {
	!(event.target instanceof HTMLInputElement || event.target instanceof HTMLTextAreaElement) && assert("[BUG] event target must be an <input> or <textarea> element", event.target instanceof HTMLInputElement || event.target instanceof HTMLTextAreaElement);
	return event.target;
}
function valueForEvent(event) {
	return elementForEvent(event).value;
}
function devirtualize(callback) {
	return (event) => callback(valueForEvent(event), event);
}
function valueFrom(reference) {
	if (reference === void 0) return new LocalValue(void 0);
	else if (isConstRef(reference)) return new LocalValue(valueForRef(reference));
	else if (isUpdatableRef(reference)) return new UpstreamValue(reference);
	else return new ForkedValue(reference);
}
var LocalValue = class {
	static {
		decorateFieldV2(this.prototype, "value", [tracked]);
	}
	#value = (initializeDeferredDecorator(this, "value"), void 0);
	constructor(value) {
		this.value = value;
	}
	get() {
		return this.value;
	}
	set(value) {
		this.value = value;
	}
};
var UpstreamValue = class {
	constructor(reference) {
		this.reference = reference;
	}
	get() {
		return valueForRef(this.reference);
	}
	set(value) {
		updateRef(this.reference, value);
	}
};
var ForkedValue = class {
	local;
	upstream;
	lastUpstreamValue = UNINITIALIZED;
	constructor(reference) {
		this.upstream = new UpstreamValue(reference);
	}
	get() {
		let upstreamValue = this.upstream.get();
		if (upstreamValue !== this.lastUpstreamValue) {
			this.lastUpstreamValue = upstreamValue;
			this.local = new LocalValue(upstreamValue);
		}
		!this.local && assert("[BUG] this.local must have been initialized at this point", this.local);
		return this.local.get();
	}
	set(value) {
		!this.local && assert("[BUG] this.local must have been initialized at this point", this.local);
		this.local.set(value);
	}
};
var AbstractInput = class extends InternalComponent {
	validateArguments() {
		!(this.args.positional.length === 0) && assert(`The ${this.constructor} component does not take any positional arguments`, this.args.positional.length === 0);
		super.validateArguments();
	}
	_value = valueFrom(this.args.named["value"]);
	get value() {
		return this._value.get();
	}
	set value(value) {
		this._value.set(value);
	}
	valueDidChange(event) {
		this.value = valueForEvent(event);
	}
	/**
	* The `change` and `input` actions need to be overridden in the `Input`
	* subclass. Unfortunately, some ember-source builds currently uses babel
	* loose mode to transpile its classes. Having the `@action` decorator on the
	* super class creates a getter on the prototype, and when the subclass
	* overrides the method, the loose mode transpilation would emit something
	* like `Subclass.prototype['change'] = function change() { ... }`, which
	* fails because `prototype['change']` is getter-only/readonly. The correct
	* solution is to use `Object.defineProperty(prototype, 'change', ...)` but
	* that requires disabling loose mode. For now, the workaround is to add the
	* decorator only on the subclass. This is more of a configuration issue on
	* our own builds and doesn't really affect apps.
	*/
	static {
		decorateMethodV2(this.prototype, "valueDidChange", [action]);
	}
	change(event) {
		this.valueDidChange(event);
	}
	input(event) {
		this.valueDidChange(event);
	}
	keyUp(event) {
		switch (event.key) {
			case "Enter":
				this.listenerFor("enter")(event);
				this.listenerFor("insert-newline")(event);
				break;
			case "Escape": this.listenerFor("escape-press")(event);
		}
	}
	static {
		decorateMethodV2(this.prototype, "keyUp", [action]);
	}
	listenerFor(name) {
		let listener = super.listenerFor(name);
		if (this.isVirtualEventListener(name, listener)) return devirtualize(listener);
		else return listener;
	}
	isVirtualEventListener(name, _listener) {
		return [
			"enter",
			"insert-newline",
			"escape-press"
		].indexOf(name) !== -1;
	}
};
/**
@module @ember/component
*/
var isValidInputType;
if (hasDOM) {
	const INPUT_TYPES = Object.create(null);
	const INPUT_ELEMENT = document.createElement("input");
	INPUT_TYPES[""] = false;
	INPUT_TYPES["text"] = true;
	INPUT_TYPES["checkbox"] = true;
	isValidInputType = (type) => {
		let isValid = INPUT_TYPES[type];
		if (isValid === void 0) {
			try {
				INPUT_ELEMENT.type = type;
				isValid = INPUT_ELEMENT.type === type;
			} catch (_e) {
				isValid = false;
			} finally {
				INPUT_ELEMENT.type = "text";
			}
			INPUT_TYPES[type] = isValid;
		}
		return isValid;
	};
} else isValidInputType = (type) => type !== "";
/**
See [Ember.Templates.components.Input](/ember/release/classes/Ember.Templates.components/methods/Input?anchor=Input).

@method input
@for Ember.Templates.helpers
@param {Hash} options
@public
*/
/**
An opaque interface which can be imported and used in strict-mode
templates to call <Input>.

See [Ember.Templates.components.Input](/ember/release/classes/Ember.Templates.components/methods/Input?anchor=Input).

@for @ember/component
@method Input
@see {Ember.Templates.components.Input}
@public
**/
/**
The `Input` component lets you create an HTML `<input>` element.

```gjs
import { Input } from '@ember/component';

<template>
<Input @value="987" />
</template>
```

creates an `<input>` element with `type="text"` and value set to 987.

### Text field

If no `type` argument is specified, a default of type 'text' is used.

```handlebars
Search:
<Input @value={{this.searchWord}} />
```

In this example, the initial value in the `<input>` will be set to the value of
`this.searchWord`. If the user changes the text, the value of `this.searchWord` will also be
updated.

### Actions

The `Input` component takes a number of arguments with callbacks that are invoked in response to
user events.

* `enter`
* `insert-newline`
* `escape-press`
* `focus-in`
* `focus-out`
* `key-down`
* `key-press`
* `key-up`

These callbacks are passed to `Input` like this:

```handlebars
<Input @value={{this.searchWord}} @enter={{this.query}} />
```

Starting with Ember Octane, we recommend using the `{{on}}` modifier to call actions
on specific events, such as the input event.

```handlebars
<label for="input-name">Name:</label>
<Input
@id="input-name"
@value={{this.name}}
{{on "input" this.validateName}}
/>
```

The event name (e.g. `focusout`, `input`, `keydown`) always follows the casing
that the HTML standard uses.

### `<input>` HTML Attributes to Avoid

In most cases, if you want to pass an attribute to the underlying HTML `<input>` element, you
can pass the attribute directly, just like any other Ember component.

```handlebars
<Input @type="text" size="10" />
```

In this example, the `size` attribute will be applied to the underlying `<input>` element in the
outputted HTML.

However, there are a few attributes where you **must** use the `@` version.

* `@type`: This argument is used to control which Ember component is used under the hood
* `@value`: The `@value` argument installs a two-way binding onto the element. If you wanted a
one-way binding, use `<input>` with the `value` property and the `input` event instead.
* `@checked` (for checkboxes): like `@value`, the `@checked` argument installs a two-way binding
onto the element. If you wanted a one-way binding, use `<input type="checkbox">` with
`checked` and the `input` event instead.

### Checkbox

To create an `<input type="checkbox">`:

```handlebars
Emberize Everything:
<Input @type="checkbox" @checked={{this.isEmberized}} name="isEmberized" />
```

This will bind the checked state of this checkbox to the value of `isEmberized` -- if either one
changes, it will be reflected in the other.

@method Input
@for Ember.Templates.components
@param {Hash} options
@public
*/
var _Input = class extends AbstractInput {
	static toString() {
		return "Input";
	}
	/**
	* The HTML class attribute.
	*/
	get class() {
		if (this.isCheckbox) return "ember-checkbox ember-view";
		else return "ember-text-field ember-view";
	}
	/**
	* The HTML type attribute.
	*/
	get type() {
		let type = this.named("type");
		if (type === null || type === void 0) return "text";
		!(typeof type === "string") && assert("The `@type` argument to the <Input> component must be a string", typeof type === "string");
		return isValidInputType(type) ? type : "text";
	}
	get isCheckbox() {
		return this.named("type") === "checkbox";
	}
	_checked = valueFrom(this.args.named["checked"]);
	get checked() {
		if (this.isCheckbox) {
			warn("`<Input @type=\"checkbox\" />` reflects its checked state via the `@checked` argument. You wrote `<Input @type=\"checkbox\" @value={{...}} />` which is likely not what you intended. Did you mean `<Input @type=\"checkbox\" @checked={{...}} />`?", untrack(() => this.args.named["checked"] !== void 0 || this.args.named["value"] === void 0 || typeof valueForRef(this.args.named["value"]) === "string"), { id: "ember.built-in-components.input-checkbox-value" });
			return this._checked.get();
		} else return;
	}
	set checked(checked) {
		warn("`<Input @type=\"checkbox\" />` reflects its checked state via the `@checked` argument. You wrote `<Input @type=\"checkbox\" @value={{...}} />` which is likely not what you intended. Did you mean `<Input @type=\"checkbox\" @checked={{...}} />`?", untrack(() => this.args.named["checked"] !== void 0 || this.args.named["value"] === void 0 || typeof valueForRef(this.args.named["value"]) === "string"), { id: "ember.built-in-components.input-checkbox-value" });
		this._checked.set(checked);
	}
	change(event) {
		if (this.isCheckbox) this.checkedDidChange(event);
		else super.change(event);
	}
	static {
		decorateMethodV2(this.prototype, "change", [action]);
	}
	input(event) {
		if (!this.isCheckbox) super.input(event);
	}
	static {
		decorateMethodV2(this.prototype, "input", [action]);
	}
	checkedDidChange(event) {
		let element = event.target;
		!(element instanceof HTMLInputElement) && assert("[BUG] element must be an <input>", element instanceof HTMLInputElement);
		this.checked = element.checked;
	}
	static {
		decorateMethodV2(this.prototype, "checkedDidChange", [action]);
	}
	isSupportedArgument(name) {
		return [
			"type",
			"value",
			"checked",
			"enter",
			"insert-newline",
			"escape-press"
		].indexOf(name) !== -1 || super.isSupportedArgument(name);
	}
};
var Input = opaquify(_Input, InputTemplate);
var TextareaTemplate = templateFactory({
	"id": null,
	"block": "[[[11,\"textarea\"],[16,1,[30,0,[\"id\"]]],[16,0,[30,0,[\"class\"]]],[17,1],[16,2,[30,0,[\"value\"]]],[4,[32,0],[\"change\",[30,0,[\"change\"]]],null],[4,[32,0],[\"input\",[30,0,[\"input\"]]],null],[4,[32,0],[\"keyup\",[30,0,[\"keyUp\"]]],null],[4,[32,0],[\"paste\",[30,0,[\"valueDidChange\"]]],null],[4,[32,0],[\"cut\",[30,0,[\"valueDidChange\"]]],null],[12],[13]],[\"&attrs\"],[]]",
	"moduleName": "packages/@ember/-internals/glimmer/lib/templates/textarea.hbs",
	"scope": () => ({ on }),
	"isStrictMode": true
});
/**
@module @ember/component
*/
var _Textarea = class extends AbstractInput {
	static toString() {
		return "Textarea";
	}
	get class() {
		return "ember-text-area ember-view";
	}
	change(event) {
		super.change(event);
	}
	static {
		decorateMethodV2(this.prototype, "change", [action]);
	}
	input(event) {
		super.input(event);
	}
	static {
		decorateMethodV2(this.prototype, "input", [action]);
	}
	isSupportedArgument(name) {
		return [
			"type",
			"value",
			"enter",
			"insert-newline",
			"escape-press"
		].indexOf(name) !== -1 || super.isSupportedArgument(name);
	}
};
var Textarea = opaquify(_Textarea, TextareaTemplate);
//#endregion
export { ComponentStateBucket as a, IS_DISPATCHING_ATTRS as c, isCurlyManager as d, processComponentInitializationAssertions as f, EventDispatcher as g, normalizeProperty as h, CURLY_COMPONENT_MANAGER as i, getComponentCapturedArgs as l, MUTABLE_CELL as m, Textarea as n, CurlyComponentManager as o, unwrapTemplate as p, BOUNDS as r, DIRTY_TAG as s, Input as t, initialRenderInstrumentDetails as u };
