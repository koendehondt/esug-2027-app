import { v as registerHandler } from "./debug-CzKtnQbf.js";
//#region node_modules/ember-cli-deprecation-workflow/src/index.js
var LOG_LIMIT = 100;
function setupDeprecationWorkflow(config) {
	self.deprecationWorkflow = self.deprecationWorkflow || {};
	self.deprecationWorkflow.deprecationLog = { messages: /* @__PURE__ */ new Set() };
	registerHandler((message, options, next) => handleDeprecationWorkflow(config, message, options, next));
	registerHandler(deprecationCollector);
	self.deprecationWorkflow.flushDeprecations = (options) => flushDeprecations({
		config,
		...options
	});
}
function matchesWorkflow(matcher, value) {
	return typeof matcher === "string" && matcher === value || matcher instanceof RegExp && matcher.exec(value);
}
function detectWorkflow(config, message, options) {
	if (!config || !config.workflow) return;
	let i, workflow, matcher, idMatcher;
	for (i = 0; i < config.workflow.length; i++) {
		workflow = config.workflow[i];
		matcher = workflow.matchMessage;
		idMatcher = workflow.matchId;
		if (matchesWorkflow(idMatcher, options?.id) || matchesWorkflow(matcher, message)) return workflow;
	}
}
function flushDeprecations({ handler = "silence", config = {} } = {}) {
	let messages = self.deprecationWorkflow.deprecationLog.messages;
	let existing = config.workflow ?? [];
	let collected = messages.values().filter((matchId) => !existing.some((entry) => entry.matchId === matchId)).map((matchId) => ({
		handler,
		matchId
	}));
	let mergedConfig = {
		...config,
		workflow: [...existing, ...collected]
	};
	return `import setupDeprecationWorkflow from 'ember-cli-deprecation-workflow';

setupDeprecationWorkflow(${JSON.stringify(mergedConfig, void 0, 2)});`;
}
function handleDeprecationWorkflow(config, message, options, next) {
	let matchingWorkflow = detectWorkflow(config, message, options);
	if (!matchingWorkflow) {
		if (config && config.throwOnUnhandled) throw new Error(message);
		else next(message, options);
	} else switch (matchingWorkflow.handler) {
		case "silence": break;
		case "log": {
			let key = options && options.id || message;
			if (!self.deprecationWorkflow.logCounts) self.deprecationWorkflow.logCounts = {};
			let count = self.deprecationWorkflow.logCounts[key] || 0;
			self.deprecationWorkflow.logCounts[key] = ++count;
			if (count <= LOG_LIMIT) {
				console.warn("DEPRECATION: " + message);
				if (count === LOG_LIMIT) console.warn("To avoid console overflow, this deprecation will not be logged any more in this run.");
			}
			break;
		}
		case "throw": throw new Error(message + ` (id: ${options?.id || "unknown"})`);
		default: next(message, options);
	}
}
function deprecationCollector(message, options, next) {
	self.deprecationWorkflow.deprecationLog.messages.add(options.id);
	next(message, options);
}
//#endregion
export { setupDeprecationWorkflow as default, deprecationCollector, detectWorkflow, flushDeprecations, handleDeprecationWorkflow };
