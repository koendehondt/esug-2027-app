import { n as __exportAll } from "./rolldown-runtime-DC62tzP2.js";
import { J as addListener, Q as sendEvent, X as on, Y as hasListeners, Z as removeListener } from "./set_properties-BghuuGJx-C1lXKl_Y.js";
import { g as Mixin } from "./object-CjD7BznD.js";
//#region node_modules/ember-source/dist/dev/packages/@ember/object/evented.js
var evented_exports = /* @__PURE__ */ __exportAll({
	default: () => Evented,
	on: () => on
});
/**
@module @ember/object/evented
*/
/**
This mixin allows for Ember objects to subscribe to and emit events.

```app/utils/person.js
import EmberObject from '@ember/object';
import Evented from '@ember/object/evented';

export default class Person extends EmberObject.extend(Evented) {
greet() {
// ...
this.trigger('greet');
}
}
```

```javascript
var person = Person.create();

person.on('greet', function() {
console.log('Our person has greeted');
});

person.greet();

// outputs: 'Our person has greeted'
```

You can also chain multiple event subscriptions:

```javascript
person.on('greet', function() {
console.log('Our person has greeted');
}).one('greet', function() {
console.log('Offer one-time special');
}).off('event', this, forgetThis);
```

@class Evented
@public
*/
var Evented = Mixin.create({
	on(name, target, method) {
		addListener(this, name, target, method);
		return this;
	},
	one(name, target, method) {
		addListener(this, name, target, method, true);
		return this;
	},
	trigger(name, ...args) {
		sendEvent(this, name, args);
	},
	off(name, target, method) {
		removeListener(this, name, target, method);
		return this;
	},
	has(name) {
		return hasListeners(this, name);
	}
});
//#endregion
export { evented_exports as n, Evented as t };
