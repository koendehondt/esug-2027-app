import { n as __exportAll } from "./rolldown-runtime-DC62tzP2.js";
import { E as tagMetaFor, T as tagFor, _ as tagForObject, a as isClassicDecorator, c as nativeDescDecorator, g as objectAt, h as markObjectAsDirty, i as descriptorForProperty, l as setClassicDecorator, n as ComputedDescriptor, o as isElementDescriptor, r as descriptorForDecorator, s as makeComputedDecorator, v as tagForProperty, w as dirtyTagFor } from "./decorator-B5Uh5NFI-CxIWwiV3.js";
import { o as deprecate } from "./debug-CzKtnQbf.js";
import { $ as valueForTag, A as VOLATILE, B as createUpdatableTag, C as ALLOW_CYCLES, D as CurrentTag, E as CURRENT_TAG, F as bump, G as isConst, H as endTrackFrame, I as combine, J as resetTracking, K as isConstTag, L as consumeTag, M as VolatileTag, N as beginTrackFrame, O as DIRTY_TAG, P as beginUntrackFrame, Q as validateTag, R as createCache, T as CONSTANT_TAG, U as endUntrackFrame, V as debug, W as getValue, X as untrack, Y as track, j as VOLATILE_TAG, k as UPDATE_TAG, q as isTracking, w as COMPUTE, z as createTag } from "./args-proxy-BNLuRFlO-EccNNvt4.js";
import { n as tracked, r as trackedData, t as TrackedDescriptor } from "./tracked-CKE4wnE6-Ci0Zx02r.js";
import { cached } from "./ember-source_@glimmer_tracking_index__js.js";
import { $ as on, A as defineProperty, B as hasUnknownProperty, C as ComputedProperty, D as changeProperties, E as beginPropertyChanges, F as notifyPropertyChange, G as SYNC_OBSERVERS, I as PROXY_CONTENT, J as flushAsyncObservers, K as activateObserver, L as _getPath, M as endPropertyChanges, N as expandProperties, O as computed, P as isComputed, Q as hasListeners, R as _getProp, S as trySet, T as autoComputed, W as ASYNC_OBSERVERS, X as revalidateObservers, Y as removeObserver, Z as addListener, a as addNamespace, b as _setProp, c as isSearchDisabled, d as removeNamespace, et as removeListener, f as setSearchDisabled, i as NAMESPACES_BY_ID, j as defineValue, k as defineDecorator, l as processAllNamespaces, n as getProperties, o as findNamespace, p as setUnprocessedMixins, q as addObserver, r as NAMESPACES, s as findNamespaces, t as setProperties, tt as sendEvent, u as processNamespace, v as DEBUG_INJECTION_FUNCTIONS, w as PROPERTY_DID_CHANGE, x as set, y as inject, z as get } from "./set_properties-BghuuGJx-DFBAJlFx.js";
import { n as LIBRARIES, r as Libraries, t as alias } from "./alias-ZcMQckQV-DZvHMBQh.js";
import { a as replace, i as removeArrayObserver, n as arrayContentDidChange, o as replaceInNativeArray, r as arrayContentWillChange, t as addArrayObserver } from "./array-Bh_kUjzy-VluBBc1S.js";
import { t as getCachedValueFor } from "./computed_cache-DmYKevAP-CPodqRph.js";
//#region node_modules/ember-source/dist/dev/packages/@ember/-internals/metal/index.js
var metal_exports = /* @__PURE__ */ __exportAll({
	ASYNC_OBSERVERS: () => ASYNC_OBSERVERS,
	ComputedDescriptor: () => ComputedDescriptor,
	ComputedProperty: () => ComputedProperty,
	DEBUG_INJECTION_FUNCTIONS: () => DEBUG_INJECTION_FUNCTIONS,
	Libraries: () => Libraries,
	NAMESPACES: () => NAMESPACES,
	NAMESPACES_BY_ID: () => NAMESPACES_BY_ID,
	PROPERTY_DID_CHANGE: () => PROPERTY_DID_CHANGE,
	PROXY_CONTENT: () => PROXY_CONTENT,
	SYNC_OBSERVERS: () => SYNC_OBSERVERS,
	TrackedDescriptor: () => TrackedDescriptor,
	_getPath: () => _getPath,
	_getProp: () => _getProp,
	_setProp: () => _setProp,
	activateObserver: () => activateObserver,
	addArrayObserver: () => addArrayObserver,
	addListener: () => addListener,
	addNamespace: () => addNamespace,
	addObserver: () => addObserver,
	alias: () => alias,
	arrayContentDidChange: () => arrayContentDidChange,
	arrayContentWillChange: () => arrayContentWillChange,
	autoComputed: () => autoComputed,
	beginPropertyChanges: () => beginPropertyChanges,
	cached: () => cached,
	changeProperties: () => changeProperties,
	computed: () => computed,
	createCache: () => createCache,
	defineDecorator: () => defineDecorator,
	defineProperty: () => defineProperty,
	defineValue: () => defineValue,
	deprecateProperty: () => deprecateProperty,
	descriptorForDecorator: () => descriptorForDecorator,
	descriptorForProperty: () => descriptorForProperty,
	eachProxyArrayDidChange: () => eachProxyArrayDidChange,
	eachProxyArrayWillChange: () => eachProxyArrayWillChange,
	endPropertyChanges: () => endPropertyChanges,
	expandProperties: () => expandProperties,
	findNamespace: () => findNamespace,
	findNamespaces: () => findNamespaces,
	flushAsyncObservers: () => flushAsyncObservers,
	get: () => get,
	getCachedValueFor: () => getCachedValueFor,
	getProperties: () => getProperties,
	getValue: () => getValue,
	hasListeners: () => hasListeners,
	hasUnknownProperty: () => hasUnknownProperty,
	inject: () => inject,
	isClassicDecorator: () => isClassicDecorator,
	isComputed: () => isComputed,
	isConst: () => isConst,
	isElementDescriptor: () => isElementDescriptor,
	isNamespaceSearchDisabled: () => isSearchDisabled,
	libraries: () => LIBRARIES,
	makeComputedDecorator: () => makeComputedDecorator,
	markObjectAsDirty: () => markObjectAsDirty,
	nativeDescDecorator: () => nativeDescDecorator,
	notifyPropertyChange: () => notifyPropertyChange,
	objectAt: () => objectAt,
	on: () => on,
	processAllNamespaces: () => processAllNamespaces,
	processNamespace: () => processNamespace,
	removeArrayObserver: () => removeArrayObserver,
	removeListener: () => removeListener,
	removeNamespace: () => removeNamespace,
	removeObserver: () => removeObserver,
	replace: () => replace,
	replaceInNativeArray: () => replaceInNativeArray,
	revalidateObservers: () => revalidateObservers,
	sendEvent: () => sendEvent,
	set: () => set,
	setClassicDecorator: () => setClassicDecorator,
	setNamespaceSearchDisabled: () => setSearchDisabled,
	setProperties: () => setProperties,
	setUnprocessedMixins: () => setUnprocessedMixins,
	tagForObject: () => tagForObject,
	tagForProperty: () => tagForProperty,
	tracked: () => tracked,
	trySet: () => trySet
});
/**
@module ember
*/
/**
Used internally to allow changing properties in a backwards compatible way, and print a helpful
deprecation warning.

@method deprecateProperty
@param {Object} object The object to add the deprecated property to.
@param {String} deprecatedKey The property to add (and print deprecation warnings upon accessing).
@param {String} newKey The property that will be aliased.
@private
@since 1.7.0
*/
function deprecateProperty(object, deprecatedKey, newKey, options) {
	function _deprecate() {
		deprecate(`Usage of \`${deprecatedKey}\` is deprecated, use \`${newKey}\` instead.`, false, options);
	}
	Object.defineProperty(object, deprecatedKey, {
		configurable: true,
		enumerable: false,
		set(value) {
			_deprecate();
			set(this, newKey, value);
		},
		get() {
			_deprecate();
			return get(this, newKey);
		}
	});
}
var EACH_PROXIES = /* @__PURE__ */ new WeakMap();
function eachProxyArrayWillChange(array, idx, removedCnt, addedCnt) {
	let eachProxy = EACH_PROXIES.get(array);
	if (eachProxy !== void 0) eachProxy.arrayWillChange(array, idx, removedCnt, addedCnt);
}
function eachProxyArrayDidChange(array, idx, removedCnt, addedCnt) {
	let eachProxy = EACH_PROXIES.get(array);
	if (eachProxy !== void 0) eachProxy.arrayDidChange(array, idx, removedCnt, addedCnt);
}
//#endregion
//#region node_modules/ember-source/dist/dev/packages/@ember/reactive/collections.js
var ARRAY_GETTER_METHODS = /* @__PURE__ */ new Set([
	Symbol.iterator,
	"concat",
	"entries",
	"every",
	"filter",
	"find",
	"findIndex",
	"flat",
	"flatMap",
	"forEach",
	"includes",
	"indexOf",
	"join",
	"keys",
	"lastIndexOf",
	"map",
	"reduce",
	"reduceRight",
	"slice",
	"some",
	"values"
]);
var ARRAY_WRITE_THEN_READ_METHODS = /* @__PURE__ */ new Set([
	"fill",
	"push",
	"unshift"
]);
function convertToInt(prop) {
	if (typeof prop === "symbol") return null;
	const num = Number(prop);
	if (isNaN(num)) return null;
	return num % 1 === 0 ? num : null;
}
var TrackedArray = class TrackedArray {
	#options;
	constructor(arr, options) {
		this.#options = options;
		const clone = arr.slice();
		const self = this;
		const boundFns = /* @__PURE__ */ new Map();
		/**
		Flag to track whether we have *just* intercepted a call to `.push()` or
		`.unshift()`, since in those cases (and only those cases!) the `Array`
		itself checks `.length` to return from the function call.
		*/
		let nativelyAccessingLengthFromWriteMethod = false;
		return new Proxy(clone, {
			get(target, prop) {
				const index = convertToInt(prop);
				if (index !== null) {
					self.#readStorageFor(index);
					consumeTag(self.#collection);
					return target[index];
				}
				if (prop === "length") {
					if (nativelyAccessingLengthFromWriteMethod) nativelyAccessingLengthFromWriteMethod = false;
					else consumeTag(self.#collection);
					return target[prop];
				}
				if (ARRAY_WRITE_THEN_READ_METHODS.has(prop)) nativelyAccessingLengthFromWriteMethod = true;
				if (ARRAY_GETTER_METHODS.has(prop)) {
					let fn = boundFns.get(prop);
					if (fn === void 0) {
						fn = (...args) => {
							consumeTag(self.#collection);
							return target[prop](...args);
						};
						boundFns.set(prop, fn);
					}
					return fn;
				}
				return target[prop];
			},
			set(target, prop, value) {
				if (self.#options.equals(target[prop], value)) return true;
				target[prop] = value;
				const index = convertToInt(prop);
				if (index !== null) {
					self.#dirtyStorageFor(index);
					self.#dirtyCollection();
				} else if (prop === "length") self.#dirtyCollection();
				return true;
			},
			getPrototypeOf() {
				return TrackedArray.prototype;
			}
		});
	}
	#collection = createUpdatableTag();
	#storages = /* @__PURE__ */ new Map();
	#readStorageFor(index) {
		let storage = this.#storages.get(index);
		if (storage === void 0) {
			storage = createUpdatableTag();
			this.#storages.set(index, storage);
		}
		consumeTag(storage);
	}
	#dirtyStorageFor(index) {
		const storage = this.#storages.get(index);
		if (storage) DIRTY_TAG(storage);
	}
	#dirtyCollection() {
		DIRTY_TAG(this.#collection);
		this.#storages.clear();
	}
};
Object.setPrototypeOf(TrackedArray.prototype, Array.prototype);
function trackedArray(data, options) {
	return new TrackedArray(data ?? [], {
		equals: options?.equals ?? Object.is,
		description: options?.description
	});
}
function trackedMap(data, options) {
	const equals = options?.equals ?? Object.is;
	const target = data instanceof Map ? new Map(data.entries()) : new Map(data ?? []);
	const collection = createUpdatableTag();
	const storages = /* @__PURE__ */ new Map();
	function storageFor(key) {
		let storage = storages.get(key);
		if (storage === void 0) {
			storage = createUpdatableTag();
			storages.set(key, storage);
		}
		return storage;
	}
	function dirtyStorageFor(key) {
		const storage = storages.get(key);
		if (storage) DIRTY_TAG(storage);
	}
	const proxy = new Proxy(target, { get(target, prop, receiver) {
		if (prop === "set") return function(key, value) {
			if (target.has(key)) {
				if (equals(target.get(key), value)) return proxy;
			}
			dirtyStorageFor(key);
			DIRTY_TAG(collection);
			target.set(key, value);
			return proxy;
		};
		if (prop === "delete") return function(key) {
			if (!target.has(key)) return false;
			dirtyStorageFor(key);
			DIRTY_TAG(collection);
			storages.delete(key);
			return target.delete(key);
		};
		if (prop === "clear") return function() {
			if (target.size === 0) return;
			storages.forEach((s) => DIRTY_TAG(s));
			storages.clear();
			DIRTY_TAG(collection);
			target.clear();
		};
		if (prop === "get") return function(key) {
			consumeTag(storageFor(key));
			return target.get(key);
		};
		if (prop === "has") return function(key) {
			consumeTag(storageFor(key));
			return target.has(key);
		};
		if (prop === "size") {
			consumeTag(collection);
			return target.size;
		}
		const value = Reflect.get(target, prop, receiver);
		if (typeof value === "function") return function(...args) {
			consumeTag(collection);
			return value.apply(target, args);
		};
		return value;
	} });
	return proxy;
}
var TrackedObject = class TrackedObject {
	#options;
	#storages = /* @__PURE__ */ new Map();
	#collection = createUpdatableTag();
	#readStorageFor(key) {
		let storage = this.#storages.get(key);
		if (storage === void 0) {
			storage = createUpdatableTag();
			this.#storages.set(key, storage);
		}
		consumeTag(storage);
	}
	#dirtyStorageFor(key) {
		const storage = this.#storages.get(key);
		if (storage) DIRTY_TAG(storage);
	}
	#dirtyCollection() {
		DIRTY_TAG(this.#collection);
	}
	/**
	* This implementation of trackedObject is far too dynamic for TS to be happy with
	*/
	constructor(obj, options) {
		this.#options = options;
		const proto = Object.getPrototypeOf(obj);
		const descs = Object.getOwnPropertyDescriptors(obj);
		const clone = Object.create(proto);
		for (const prop in descs) Object.defineProperty(clone, prop, descs[prop]);
		const self = this;
		return new Proxy(clone, {
			get(target, prop) {
				self.#readStorageFor(prop);
				return target[prop];
			},
			has(target, prop) {
				self.#readStorageFor(prop);
				return prop in target;
			},
			ownKeys(target) {
				consumeTag(self.#collection);
				return Reflect.ownKeys(target);
			},
			set(target, prop, value) {
				if (self.#options.equals(target[prop], value)) return true;
				target[prop] = value;
				self.#dirtyStorageFor(prop);
				self.#dirtyCollection();
				return true;
			},
			deleteProperty(target, prop) {
				if (prop in target) {
					delete target[prop];
					self.#dirtyStorageFor(prop);
					self.#storages.delete(prop);
					self.#dirtyCollection();
				}
				return true;
			},
			getPrototypeOf() {
				return TrackedObject.prototype;
			}
		});
	}
};
function trackedObject(data, options) {
	return new TrackedObject(data ?? {}, {
		equals: options?.equals ?? Object.is,
		description: options?.description
	});
}
function trackedSet(data, options) {
	const equals = options?.equals ?? Object.is;
	const target = new Set(data ?? []);
	const collection = createUpdatableTag();
	const storages = /* @__PURE__ */ new Map();
	function storageFor(key) {
		let storage = storages.get(key);
		if (storage === void 0) {
			storage = createUpdatableTag();
			storages.set(key, storage);
		}
		return storage;
	}
	function dirtyStorageFor(key) {
		const storage = storages.get(key);
		if (storage) DIRTY_TAG(storage);
	}
	const proxy = new Proxy(target, { get(target, prop, receiver) {
		if (prop === "add") return function(value) {
			if (target.has(value)) {
				if (equals(value, value)) return proxy;
			} else DIRTY_TAG(collection);
			dirtyStorageFor(value);
			target.add(value);
			return proxy;
		};
		if (prop === "delete") return function(value) {
			if (!target.has(value)) return false;
			dirtyStorageFor(value);
			DIRTY_TAG(collection);
			storages.delete(value);
			return target.delete(value);
		};
		if (prop === "clear") return function() {
			if (target.size === 0) return;
			storages.forEach((s) => DIRTY_TAG(s));
			DIRTY_TAG(collection);
			storages.clear();
			target.clear();
		};
		if (prop === "has") return function(value) {
			consumeTag(storageFor(value));
			return target.has(value);
		};
		if (prop === "size") {
			consumeTag(collection);
			return target.size;
		}
		const value = Reflect.get(target, prop, receiver);
		if (typeof value === "function") return function(...args) {
			consumeTag(collection);
			return value.apply(target, args);
		};
		return value;
	} });
	return proxy;
}
function trackedWeakMap(data, options) {
	const equals = options?.equals ?? Object.is;
	const existing = data ?? [];
	/**
	* SAFETY: note that when passing in an existing weak map, we can't
	*         clone it as it is not iterable and not a supported type of structuredClone
	*/
	const target = existing instanceof WeakMap ? existing : new WeakMap(existing);
	const storages = /* @__PURE__ */ new WeakMap();
	function storageFor(key) {
		let storage = storages.get(key);
		if (storage === void 0) {
			storage = createUpdatableTag();
			storages.set(key, storage);
		}
		return storage;
	}
	function dirtyStorageFor(key) {
		const storage = storages.get(key);
		if (storage) DIRTY_TAG(storage);
	}
	const proxy = new Proxy(target, { get(target, prop, receiver) {
		if (prop === "set") return function(key, value) {
			if (target.has(key)) {
				if (equals(target.get(key), value)) return proxy;
			}
			dirtyStorageFor(key);
			target.set(key, value);
			return proxy;
		};
		if (prop === "delete") return function(key) {
			if (!target.has(key)) return false;
			dirtyStorageFor(key);
			storages.delete(key);
			return target.delete(key);
		};
		if (prop === "get") return function(key) {
			consumeTag(storageFor(key));
			return target.get(key);
		};
		if (prop === "has") return function(key) {
			consumeTag(storageFor(key));
			return target.has(key);
		};
		const value = Reflect.get(target, prop, receiver);
		if (typeof value === "function") return value.bind(target);
		return value;
	} });
	return proxy;
}
/**
* NOTE: we cannot pass a WeakSet because WeakSets are not iterable
*/
/**
* Creates an instanceof WeakSet from an optional list of entries
*
*/
function trackedWeakSet(data, options) {
	const equals = options?.equals ?? Object.is;
	const target = new WeakSet(data ?? []);
	const storages = /* @__PURE__ */ new WeakMap();
	function storageFor(key) {
		let storage = storages.get(key);
		if (storage === void 0) {
			storage = createUpdatableTag();
			storages.set(key, storage);
		}
		return storage;
	}
	function dirtyStorageFor(key) {
		const storage = storages.get(key);
		if (storage) DIRTY_TAG(storage);
	}
	const proxy = new Proxy(target, { get(target, prop, receiver) {
		if (prop === "add") return function(value) {
			/**
			* In a WeakSet, there is no `.get()`, but if there was,
			* we could assume it's the same value as what we passed.
			*
			* So for a WeakSet, if we try to add something that already exists
			* we no-op.
			*
			* WeakSet already does this internally for us,
			* but we want the ability for the reactive behavior to reflect the same behavior.
			*
			* i.e.: doing weakSet.add(value) should never dirty with the defaults
			*       if the `value` is already in the weakSet
			*/
			if (target.has(value)) {
				if (equals(value, value)) return proxy;
			}
			target.add(value);
			dirtyStorageFor(value);
			return proxy;
		};
		if (prop === "delete") return function(value) {
			if (!target.has(value)) return false;
			dirtyStorageFor(value);
			storages.delete(value);
			return target.delete(value);
		};
		if (prop === "has") return function(value) {
			consumeTag(storageFor(value));
			return target.has(value);
		};
		const value = Reflect.get(target, prop, receiver);
		if (typeof value === "function") return value.bind(target);
		return value;
	} });
	return proxy;
}
//#endregion
//#region node_modules/ember-source/dist/dev/packages/@glimmer/validator/index.js
var validator_exports = /* @__PURE__ */ __exportAll({
	ALLOW_CYCLES: () => ALLOW_CYCLES,
	COMPUTE: () => COMPUTE,
	CONSTANT: () => 0,
	CONSTANT_TAG: () => CONSTANT_TAG,
	CURRENT_TAG: () => CURRENT_TAG,
	CurrentTag: () => CurrentTag,
	INITIAL: () => 1,
	VOLATILE: () => VOLATILE,
	VOLATILE_TAG: () => VOLATILE_TAG,
	VolatileTag: () => VolatileTag,
	beginTrackFrame: () => beginTrackFrame,
	beginUntrackFrame: () => beginUntrackFrame,
	bump: () => bump,
	combine: () => combine,
	consumeTag: () => consumeTag,
	createCache: () => createCache,
	createTag: () => createTag,
	createUpdatableTag: () => createUpdatableTag,
	debug: () => debug,
	dirtyTag: () => DIRTY_TAG,
	dirtyTagFor: () => dirtyTagFor,
	endTrackFrame: () => endTrackFrame,
	endUntrackFrame: () => endUntrackFrame,
	getValue: () => getValue,
	isConst: () => isConst,
	isConstTag: () => isConstTag,
	isTracking: () => isTracking,
	resetTracking: () => resetTracking,
	tagFor: () => tagFor,
	tagMetaFor: () => tagMetaFor,
	track: () => track,
	trackedArray: () => trackedArray,
	trackedData: () => trackedData,
	trackedMap: () => trackedMap,
	trackedObject: () => trackedObject,
	trackedSet: () => trackedSet,
	trackedWeakMap: () => trackedWeakMap,
	trackedWeakSet: () => trackedWeakSet,
	untrack: () => untrack,
	updateTag: () => UPDATE_TAG,
	validateTag: () => validateTag,
	valueForTag: () => valueForTag
});
var GLIMMER_VALIDATOR_REGISTRATION = Symbol("GLIMMER_VALIDATOR_REGISTRATION");
if (Reflect.has(globalThis, GLIMMER_VALIDATOR_REGISTRATION)) throw new Error("The `@glimmer/validator` library has been included twice in this application. It could be different versions of the package, or the same version included twice by mistake. `@glimmer/validator` depends on having a single copy of the package in use at any time in an application, even if they are the same version. You must dedupe your build to remove the duplicate packages in order to prevent this error.");
Reflect.set(globalThis, GLIMMER_VALIDATOR_REGISTRATION, true);
//#endregion
export { metal_exports as n, validator_exports as t };
