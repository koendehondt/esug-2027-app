"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.ResolverLoader = void 0;
const fs_extra_1 = require("fs-extra");
const module_resolver_options_1 = require("./module-resolver-options");
const module_resolver_1 = require("./module-resolver");
const shared_internals_1 = require("@embroider/shared-internals");
const path_1 = require("path");
const fs_1 = require("fs");
class ResolverLoader {
    appRoot;
    #resolver;
    #configFile;
    #watcher;
    constructor(appRoot, watch = false) {
        this.appRoot = appRoot;
        this.#configFile = (0, path_1.join)((0, shared_internals_1.locateEmbroiderWorkingDir)(this.appRoot), 'resolver.json');
        if (watch) {
            this.#watcher = (0, fs_1.watch)(this.#configFile, { persistent: false }, () => {
                this.#resolver = undefined;
            });
        }
    }
    close() {
        this.#watcher?.close();
    }
    get resolver() {
        if (!this.#resolver) {
            let config;
            if ((0, fs_extra_1.existsSync)(this.#configFile)) {
                let rawConfig = (0, fs_extra_1.readJSONSync)(this.#configFile);
                rawConfig.splitAtRoutes = rawConfig.splitAtRoutes?.map((splitRouteConfig) => {
                    if (splitRouteConfig.type === 'regex') {
                        const fragments = splitRouteConfig.value.match(/^\/(.*)\/([gimsuy]*)$/);
                        if (!fragments) {
                            throw new Error(`Unable to parse splitAtRoutes pattern ${splitRouteConfig.value}`);
                        }
                        const [, parsedPattern, parsedFlags] = fragments;
                        return new RegExp(parsedPattern, parsedFlags);
                    }
                    return splitRouteConfig.value;
                });
                config = rawConfig;
            }
            else {
                config = (0, module_resolver_options_1.buildResolverOptions)({});
            }
            this.#resolver = new module_resolver_1.Resolver(config);
        }
        return this.#resolver;
    }
}
exports.ResolverLoader = ResolverLoader;
//# sourceMappingURL=resolver-loader.js.map